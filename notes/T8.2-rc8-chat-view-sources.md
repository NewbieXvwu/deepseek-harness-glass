# T8.2：RC8 ChatView 与基础消息 Renderer 来源及收敛记录

**锁定基线：** `deepseek-ai/deepseek-harness@141eb6fef83422698aef7a981029e843e8161534`（`dsh-v0.1.0-rc.8`）。本任务只覆盖 stable Chat flow、Host durable user/assistant text nodes、streaming tail、copy/time chrome、running turn 状态和 history/live 的共同 keyed row tree。Markdown/代码/链接安全策略留给 T8.3；composer 行为留给 T8.4/T8.5；context/thinking/retry/turn failure/tool/trajectory 等节点继续由 T6.6/T9 负责，不能以伪摘要填充。

| 官方来源 | RC8 约束 | 原生实现边界 |
|---|---|---|
| `packages/client/ui-conversation/src/client/chat/ChatView.tsx:8-14, 158-243` | flow order 仅在 node 进入、离开、移动时变化；每个 keyed seat 单独订阅，assistant streaming update 只刷新自身 row，不能重建全页。 | 保留 `NativeSessionStore.TranscriptItem.id/sequence` 和 `LazyVStack` keyed identity；为 item 加入 Host event time，但不从 UI 重算 durable order。 |
| `ChatView.module.css:14-45, 47-58` | scroll body full-bleed、column max width 居中、`gap:16px`；无内容 keyed row 不占 gap。 | transcript 保持 native `ScrollViewReader` 与 resident composer 分离；更新 chat row gap 和 container padding/column rules 以贴近 RC8。 |
| `MessageItem.tsx:215-301` 与 `MessageItem.module.css:4-35` | user message right-align，bubble 最大 `min(525px, 82%)`、22px radius；durable user/steering 一律复用同一 bubble。 | T8.2 只投影 `TranscriptItem.role == user` 的 Host durable plain text；references/images/extra blocks 留给附件与 node renderer 任务。 |
| `MessageIconActions.tsx:46-140` | copy 有 1 秒 copied-check transient state；time 仅来自 durable event；user clock 在 icons 前，assistant 在 icons 后；run/TTFT/tok/s 只在 Host metrics 已给出时显示。 | Native copy 只写 macOS `NSPasteboard`；时间直接使用 `SessionEventDTO.time`。未拥有 Host metrics 时不编造 run/TTFT/tokens 文案。 |
| `ChatView.tsx:118-151`、`ChatView.module.css:67-115` | running turn 使用 `Deep diving...` live status；长于 15 秒才显示运行 clock；减少动态时停止 shimmer。 | 首个原生 tail 仅用 `isStreaming`/`isRunning` 的 typed Core state 显示 status，并保持 stable row；精确 duration 与 reduced-motion细节作为后续同状态测试扩展。 |

## 当前 Jobs 场景差异与 T8.2 切片

`9176170` 的 macOS-26 run `32338169145` 已使 Jobs header 对齐到 official session title/preset/jobs action，但 native body 仍只有 user/assistant plain text。官方 Jobs fixture还包含 Context injection、Think、assistant action/time row和 terminal `This turn failed` node；本轮只能可靠收敛用户气泡、assistant plain text 与 copy/time/streaming，不能把 context/thinking/error 粗暴合成普通消息。完成后仍须保持 Jobs `report-only`，并把未实现视觉差异显式分类为 T6.6/T9/T8.3–T8.7。

## References

[1] https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-conversation/src/client/chat/ChatView.tsx

[2] https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-conversation/src/client/chat/ChatView.module.css

[3] https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-conversation/src/client/chat/MessageItem.tsx

[4] https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-conversation/src/client/chat/MessageIconActions.tsx
