# T7.1：RC8 SidebarRoot 原生结构来源与收敛记录

**锁定基线：** `deepseek-ai/deepseek-harness@141eb6fef83422698aef7a981029e843e8161534`（`dsh-v0.1.0-rc.8`）。本记录只覆盖 expanded sidebar shell 的本轮可见结构修复；T7.1 仍未勾选，直至 RC8 浅色/深色同状态截图、ARIA/geometry、人工差异分类和该提交自身 macOS-26 CI 全部闭环。

| 官方来源 | 约束 | 原生映射 |
|---|---|---|
| `packages/client/ui-sidebar/src/client/SidebarRoot.tsx:128-188` | expanded brand 是可开始新会话的 runtime fallback identity；fallback 文本是 `DSH Local Build`，提交短码仅在构建变量存在时显示。 | `NativeSidebarView.wideBrand` 使用 `fish-logo`、`OfficialUISpec.Text.sidebarFallbackBrand`、锁定 `Build.sourceCommit` 的 7 位投影和同一 `onNewSession` action。 |
| `SidebarRoot.module.css:87-154` | expanded logo row 60px，brand mark 24px，brand name 17px/600、24px line-height，badge 16px。 | `OfficialUISpec.Layout.sidebarLogoRowHeight/sidebarBrandMarkSize/sidebarBuildBadgeHeight` 与 `Typography.sidebarBrand17/sidebarBuildBadge8`；无 UI 文本或颜色字面量。 |
| `SidebarRoot.module.css:227-247` | expanded New Session 为 38px 高、12px radius、14px/500、16px horizontal padding、2px horizontal/8px bottom margin。 | `NativeSidebarView.newSessionButton` 采用 `sidebarNewSessionHeight`；既有官方 button style、icon 和 locale label 保留。 |

`official-ui-catalog.json` 已将 `SidebarRoot.tsx`、`SidebarRoot.module.css` 加入 RC8 source inputs，并登记新文本/几何来源；`OfficialUISpecBuildTests` 锁定 fallback 文本、7位 revision 和四项关键 geometry。此实现没有改写 Host workspace/session truth、没有引入 WebView，也没有把系统 sidebar 材质替换为自绘背景。

## 待验证

本轮变更尚未完成本地静态门禁与本提交自身的 macOS-26 CI。成功后需下载 `welcome-no-workspace-{light,dark}` 与 `jobs-expanded-{light,dark}` 工件，核对 brand baseline、badge、New Session 高度、rail toggle、区域 padding、dark token 及 visual diff 指标。完整 workspace/session rows、搜索和管理交互仍分别属于 T7.2/T7.3；session header、tabs、transcript 和 composer 仍分别属于 T8/T9，不得用本记录提前勾选任何任务。

## References

[1] https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-sidebar/src/client/SidebarRoot.tsx

[2] https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-sidebar/src/client/SidebarRoot.module.css


### Brand 宽度 failure closure（待 CI）

`0b1f492` 的 macOS-26 [run 32334041710](https://github.com/NewbieXvwu/deepseek-harness-glass/actions/runs/32334041710) 成功通过编译、XCTest和截图链，但 RC8 native welcome PNG 显示 `DSH Local Build` 被截断为 `DSH Local B...`。官方 `SidebarRoot.module.css:.brand` 的 `flex: 1; min-width: 0` 语义要求品牌组独占 toggle 之前的剩余空间；原生 `wideHeader` 同时放置 `.frame(maxWidth: .infinity)` 的 `wideBrand` 与额外 `Spacer`，两者竞争剩余宽度而压缩文本。修复移除该非官方 Spacer 并给 `wideBrand` 高 layout priority，保留 28px toggle 和 8px官方 gap。必须由新的提交自身 macOS-26 run 重新生成 light/dark welcome 与 Jobs pair，且仍不得提前勾选 T7.1。


### AppKit sidebar leading composition closure（待 CI）

`55514e4` 的 macOS-26 [run 32334515699](https://github.com/NewbieXvwu/deepseek-harness-glass/actions/runs/32334515699) 成功通过完整回归，但 native welcome image 仍显示 sidebar 内所有 child（brand、New Session、Workspaces、Settings）整体比 RC8 官方图右移约 7px，且 brand 继续截断。这不是缩小名称或移除 badge 的理由：官方图在同一 280px column 内完整展示 brand。对官方 CSS root 12px inset 与同视口 WindowServer pair 的测量表明 `NSSplitViewItem.sidebar` 的系统组合额外占用约 7px leading visual inset。原生 expanded sidebar 因而使用受控 `sidebarNativeExpandedLeadingInset=5`，trailing 仍保留官方 12px；rail 的 10px 对称 inset 不变。该 native composition constant 已登记 catalog、`OfficialUISpec` 和 XCTest，必须由新的提交自身 light/dark pair 复核实际边缘及 brand 全文显示。


### Leading composition 验证结果

纠正提交 `92941c9` 的 macOS-26 [run 32335006726](https://github.com/NewbieXvwu/deepseek-harness-glass/actions/runs/32335006726) 成功完成 RC8 官方 capture、D0/D1/架构门禁、独立 SwiftPM 编译、全量 XCTest、native app 装配、WindowServer snapshot 和所有 pair diff。其 `welcome-light.png` 现在完整显示 `DSH Local Build` 与 `141eb6f` badge，fish、New Session、Workspaces、Settings 左边缘亦与官方 280px sidebar 结构对齐；此前全体 child 的右移和 brand ellipsis 已关闭。

本轮 welcome light 指标为 material `0.02957868`、mean `3.192088`、exact `0.51232794`；Jobs light 为 material `0.16258464`、mean `28.472418`、exact `0.41434710`。数值仍远高于严格 enforce 阈值，原因主要位于尚未原生收敛的 hero/composer、session header/tabs/transcript、系统材质与后续 interaction surface，而非本次 sidebar identity/edge 修复。T7.1、T5.* 与 T6.6 仍保持未勾选；下一轮必须从相应 UI TODO 的官方组件和同状态 capture 继续，不能把成功编译或局部对齐误报为场景验收。


### Footer leading 复核（待修复）

在 `32335006726` 的同状态 pair 目视核验中，修复后的 fish、brand、New Session 和 Workspaces header 已落在 RC8 官方的 280px expanded sidebar 轴线上；但 native `Settings` gear/label 的可见左缘仍约比官方提前 5px。官方 `SidebarRoot.tsx:199-207` 表明 Settings 是 `footArea → settingsArea → sidebar.settings` 的独立 injected slot，不应由上方 header/workspace 的布局补偿掩盖。审计脚本 `glass/scripts/measure_sidebar_footer.py` 在锁定 light pair 上量得 official foreground `x=19,y=801…816`、native foreground `x=14,y=803…817`，即确有 5px 横向偏差（同时存在 2px 基线差，留待同场景的整体垂直收敛处理）。原生 `settingsButton` 现以 catalog 登记的 `sidebarNativeExpandedFooterLeadingAdjustment=5` 仅补偿 expanded Settings label；collapsed rail 仍保持其官方 36px 居中几何。该修复的 Xunit/SwiftPM 测试、light/dark 同状态截图与该提交专属 macOS-26 CI 尚待闭环，故不把第 36 行的“Settings 左边缘对齐”作为完成性断言。
