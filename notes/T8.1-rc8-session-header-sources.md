# T8.1：RC8 Session Header 与 View Tabs 来源及收敛记录

**锁定基线：** `deepseek-ai/deepseek-harness@141eb6fef83422698aef7a981029e843e8161534`（`dsh-v0.1.0-rc.8`）。本记录只覆盖会话 column 顶端 strict session header、breadcrumb、tabs、header action/utilities seat 与 blank-session 的隐藏语义；transcript 的完整节点、composer、模型/权限与 jobs 详情分别继续归属于 T8.2–T8.7、T6.6、T9。

| 官方来源 | RC8 约束 | 原生实现切片 |
|---|---|---|
| `packages/client/ui-conversation/src/client/skeleton/ConversationRoot.tsx:186-193` | session header 是 resident column 的独立 sibling，位于唯一 scroll body 前；composer seat 始终在该 scroll body 中。 | 保持 `NativeActiveConversationSurface` 的单一 root 与 resident composer，不用 header/tabs 切换重建 composer。 |
| `ConversationSession.tsx:61-130` | 仅当 `blank && composerPhase == blank` 时隐藏 header；其他 session 显示 ancestry、actions、utilities，且仅在 tabs 超过一个时渲染 tablist。 | 以 `NativeWorkspaceStore.Snapshot` 的选中 `SessionSummaryDTO` 和 `NativeSessionStore.phase` 生成 typed header model；blank session 不显示空白 header。 |
| `ConversationSession.tsx:23-30, 110-124` 与 `contract/views.ts:10-30` | view id 缺失或已失效时稳定回退 `chat`；tab 由 `conversation.view` registry 的 `id/label` 投影，使用 `role=tablist/tab` 和 `aria-selected`。 | 原生 view registry 初始必须提供 Chat；为 T9 trajectory/未来插件预留受控注册表，而非将 tabs 与 Chat renderer 强耦合。 |
| `ConversationRoot.module.css:.header/.titleRow/.tabs/.tab`（28-178） | header padding `12px 28px 0 20px`，title min-height `32px`，tabs 35px strip、36px gap、8px left padding，active bar 2px，已选文本为 business blue。 | 新增 `OfficialUISpec` RC8 geometry/token 映射并以 SwiftUI 原生 header 实现；系统 navigation-only glass 不覆盖 content header。 |
| `contract/slots.ts:71-103` 与 `packages/client/ui-jobs/src/client/index.ts:31-36` | header owns title/tabs/action row；utilities 是独立右对齐 seat；view 是 session-scoped list，active view 一次只渲染一个。Jobs 明确注册在 title-adjacent `conversation.session.header.actions`，不是 utility。 | 将 `NativeJobsHeaderAction` 迁移到 title-adjacent actions seat；右侧 Session log utility 必须在其组件/locale 来源单独定位前保持未实现，不能以无来源按钮填充。 |

## Jobs 同状态基线差异

macOS-26 [run 32336004226](https://github.com/NewbieXvwu/deepseek-harness-glass/actions/runs/32336004226) 的 `jobs-expanded-light` official/native pair 显示，当前 native 顶部只有 56px 简化 `Chat` 标题行，缺少官方的 session title（`Reply with the single word…`）、agent preset、header action seat、session-log utility、35px `Chat / Trajectory` tab strip 和 2px active blue bar。原生 transcript 也只呈现 user/assistant 文本，而官方 fixture 含 Context injection、Think、copy/feedback row 与 turn failed node；composer 同样仍为简化卡。这些是实际差异，不能由 Jobs popover 的正确 locale 或 T7 sidebar 对齐抵消。

当前 `NativeWorkspaceStore.loadSnapshotFixtureWorkspace()` 已提供 Host-shaped selected `fx-alpha` summary（title `Fixture 历史会话`），而 official jobs capture 使用隔离 fixture 自身的 `Reply with the single word…` title。T8.1 不得伪造或硬编码 latter；应先以 Host summary `displayTitle` 为唯一 production/snapshot header truth，并在后续 snapshot-contract work 中将 native/official fixture title source 统一。`NativeSessionStore` 已保证 resident session restore preserves draft/items across switch，header/tab state must not invalidate that guarantee.

## 未完成验收

T8.1 仍未勾选。下一次实现至少要包含：源码 catalog/geometry/text 映射、typed header/view registry、blank chrome visibility、current/stale view fallback、session switch state preservation XCTest、accessibility role/selection assertions、light/dark jobs same-state capture 与其提交专属 macOS-26 CI。只有 Jobs 场景从 `report-only` 达到相应可解释/阈值闭环后，才可考虑勾选本项；T8.2–T8.7 和 T9 造成的其余视觉差异必须继续保持显式未完成。

## References

[1] https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-conversation/src/client/skeleton/ConversationRoot.tsx

[2] https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-conversation/src/client/skeleton/ConversationSession.tsx

[3] https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-conversation/src/client/skeleton/ConversationRoot.module.css

[4] https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-conversation/src/client/contract/views.ts

[5] https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-conversation/src/client/contract/slots.ts


## 已核验的补充来源

RC8 `packages/client/ui-jobs/src/client/index.ts:28-39` 明确把 Jobs 以 `id='job-list'`、`order=20` 注册至 `conversation.session.header.actions`；`NativeJobsHeaderAction` 必须因此处于 title-adjacent action row，而不是 right-side utility。`packages/client/ui-agent-preset/src/client/AgentPresetLabel.tsx:42-63` 仅在 session summary 的 `agentPreset` 存在时显示只读 label；其 `locales.ts:169-191` 为 system `standard` 映射 `presetStandardName`（en 为 `Standard mode`），未知 roster item 回退 id。T8.1 的本轮 native jobs fixture 因而将 captured Host summary 投影为 `agentPreset='standard'`，但生产路径仍直接读取 Host `SessionSummaryDTO.agentPreset`。

官方 [jobs capture script](https://github.com/NewbieXvwu/deepseek-harness-glass/blob/main/tools/reference-capture/capture-official-welcome.e2e.ts) 的 `recordedPrompt`（第 11 行）创建 session；`32336004226` 的 locked `jobs-expanded-light.json` 记录 `geometry.title = "Reply with the single word — DSH Local Build"`，同一 ARIA/text snapshot 记录 session durable label `Reply with the single word`、`Standard mode`、`Chat` 和 `Trajectory`。RC8 [DocumentTitle.tsx](https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-renderer/src/client/DocumentTitle.tsx) 第 17–22 行规定 selected durable title 使用 `${title} — DSH Local Build`，无 selection 则恢复 `DSH Local Build`；原生 shell 的隐藏 AppKit titlebar 同样必须维持这个 document state。

RC8 [ui-trajectory registration](https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-trajectory/src/client/index.ts) 第 43–64 行以 `id='trajectory'`、`order=10` 注册第二个 view tab。当前 native 尚未具备 T9.5 的真实 trajectory target renderer，故 T8.1 当前 registry 只注册可渲染的 `chat`；它已实现 unknown retained id→Chat fallback 和 per-resident-session view id 保持，但**不得**把尚无 renderer 的 Trajectory tab 伪装为可用。与官方 jobs screenshot 的 Trajectory tab 差异继续明确归属 T9.5/T8.1 的后续插件 view 接入，不能在本轮视为完成。


## `7e335ac` / `9176170` 初始实现验证

`7e335ac` 的 macOS-26 [run 32337992661](https://github.com/NewbieXvwu/deepseek-harness-glass/actions/runs/32337992661) 在 `Independently compile declared SwiftPM targets` 阶段失败，原因是 `NativeShellController.renderPresentation` 的 `openSession` closure 未显式捕获 `self.presentation`。失败没有被忽略；`9176170` 以 `[weak self]` 及 local current presentation 修复，同时避免 view/controller retain cycle。

修复提交 `9176170` 的 macOS-26 [run 32338169145](https://github.com/NewbieXvwu/deepseek-harness-glass/actions/runs/32338169145) **success**：official specification gates、native architecture gates、独立 SwiftPM target 编译、全部 XCTest、app assembly、四个 WindowServer snapshot 与 visual compare 都完成。专属 native `jobs-expanded-light/dark` 工件显示，header 已从旧的单独 `Chat` 行改为 Host-derived `Reply with the single word` durable title、官方 `Standard mode` preset label 和 title-adjacent background Jobs action；Jobs popover 在 light/dark 都保持可见且 locale 沿用 en-US capture contract。window/document title 也已按 RC8 `DocumentTitle` 建模。

| 场景 | `9176170` materiallyChangedRatio | 与 `ae6d743` 相比 | 人工分类 |
|---|---:|---:|---|
| `welcome-no-workspace-light` | 0.02954148 | +0.00013765 | 非 T8 header 场景的微小 compositor/文本栅格噪声；仍为 report-only。 |
| `welcome-no-workspace-dark` | 0.04995443 | +0.00017497 | 同上；仍为 report-only。 |
| `jobs-expanded-light` | 0.06673549 | **-0.09595703** | title/preset/action 已显著缩小首屏差异；残余为 Session log、Trajectory tab、完整 transcript rows、composer/footer。 |
| `jobs-expanded-dark` | 0.29248233 | **-0.08280413** | 同样改善；深色剩余主因仍为未完成 T8.2/T8.4/T8.6/T8.7 和 T9 renderer，不得提前转 enforce。 |

本轮只交付 T8.1 的基础部分，**T8.1 不勾选**：真正的 plugin view registry 尚待 T9.5 trajectory renderer，right utility Session log 的官方组件来源尚未定位，且 Jobs 场景仍是 `report-only`、未满足 `mustEnforceBeforeTodoCompletion`。下一步应在不伪造 Trajectory tab 的前提下，先实现 T8.2 durable transcript/node rows，再让已实现的 registry 接入真实 renderer 与 tablist。
