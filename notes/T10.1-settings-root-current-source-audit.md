# T10.1：Settings Root 当前来源审计

**官方基线：** `deepseek-ai/deepseek-harness@b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`（`dsh-v0.1.1-rc.2`）。本文只记录 settings trigger、section ledger、panel chrome与导航所有权，不能以源码或上游测试替代本项目 current-SHA 的 macOS-26 AppKit runtime、AX或visual证据。

## rc.2 锁定契约

官方 `SettingsRoot` 自行持有 `open` 与 active section ID：`close`同时关闭并清除active，`openSection`同时设置section并打开panel。以有序section ledger生成nav rows；用户无显式选择地打开或当前entry注销时，panel回退到first row；ledger为空时仅渲染空content column，不伪造产品section。[1]

> SettingsPanel的dialog accessibility name来自header seat ID；shell-owned close button使用`settings.close` seat的可见性隐藏label。打开时焦点直接落到close button，mask、header close和document Escape均关闭panel。[1]

| 契约 | 当前原生映射 | 仍待闭环 |
|---|---|---|
| 官方close文案 | Native toolbar close现读取`ui-settings-general.close`，不再错误借用`ui-settings-plugins.collapse`。 | current-SHA AX验证可访问名称。 |
| 初始focus | `@FocusState`在NativeSettingsRoot appear时聚焦shell-owned close Button；父级负责AppKit window创建。 | native window首次打开/重新激活的runtime focus工件。 |
| close/Escape | `NativeShellController`的独立settings `NSWindow`通过toolbar close及`windowWillClose`回写`settingsPresented=false`；AppKit closable window承担Escape/cancel标准操作。 | current-SHA close/Escape集成测试。 |
| official content boundary | 移除了本地`Plugin profile`、`Use an isolated plugin profile`及DSH_HOME说明，它们不是rc.2 SettingsRoot/locale来源。 | native root当前仅登记有已审计原生body的builtin sections；Host/plugin未知section仍须有单独admission，不能借ledger伪造页面。 |
| active fallback/empty ledger | `NativeSettingsSectionLedger`以stable order投影reviewed registrations，`NativeSettingsRoot`登记官方`general=0`、`models=10`、`plugins=15`、`agent-presets=20`；active ID只在仍登记时保留，否则回退first；空ledger为nil，detail不生成section。toolbar close通过`closedSelection()`清active，appear重新推导first active。 | current-SHA macOS navigation/unregister/empty AX与paired visual；无动态Host/plugin registration前，builtin集保持受审计封闭。 |

2026-08-22 已在Node `24.19.0`、pnpm `11.7.0`下运行rc.2 `packages/client/ui-settings-general/tests/settings-root.client.spec.tsx`，**17项测试通过**。它覆盖trigger accessible name/rail、dialog label、header actions/close、mask/Escape/focus、first-active/switch/unregister fallback/empty ledger和onboarding ownership；仅为官方行为来源，不替代native验收。[2]

2026-08-22 已新增Foundation-only `NativeSettingsSectionLedger`和`NativeSettingsSectionLedgerTests`：相同order保持registration sequence、显式active仅在仍存在时保留、nil/已注销entry回退有序first、空rows与close均返回nil。`NativeSettingsRoot`使用该投影登记四个rc.2 builtin id（包括精确`agent-presets`连字符ID），而非`CaseIterable`直接决定导航。`portable-checks`亦以独立Swift CLI harness执行同一production source；Linux完整SwiftPM XCTest会在`GlassSpec`需要AppKit时失败，这是平台缺失而非ledger compile/test failure，macOS-26 `native-ui`仍是Core XCTest与原生界面的权威门禁。

## References

[1]: https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/client/ui-settings-general/src/client/SettingsRoot.tsx "rc.2 SettingsRoot"

[2]: https://github.com/deepseek-ai/deepseek-harness/blob/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e/packages/client/ui-settings-general/tests/settings-root.client.spec.tsx "rc.2 SettingsRoot tests"
