# T6.6 下一 renderer 调研检查点

> **锁定规格来源**：`deepseek-ai/deepseek-harness@141eb6fef83422698aef7a981029e843e8161534`。

## Deliverables

已实现并推送 `a13002f`。官方 `turn-deliverables.ts` 将成功 mutation tool locations保留为 state-only turn data，`ProducedFiles.tsx` 以 closing assistant sequence裁剪、最多六个 basename chips并调用 Host opener。原生使用 reducer location seam与既有 `openKnownProjectPath`，不读 raw history或本地文件系统。

## Subagent

RC8 `ui-subagent/src/client/index.ts:60-77` 将 subagent catalog 注册到 `conversation.session.header.actions`（order 10），并在 composer seat注册 one-shot/parent-unavailable read-only takeover。`SubagentCatalogAction.tsx` 依赖 Host-authoritative `subagentsByParent` catalog、session summaries及 timing projection；本地 transport/workspace store尚无该 catalog RPC/state，不得仅凭 summaries猜造子树。

## Workflow

RC8 `ui-workflow-run/src/client/WorkflowRunPanel.tsx` 是 `conversation.chat.node` 的 `workflow-run` renderer。它依据 typed run、phase、member statuses显示可折叠 panel，且只对仍运行、确属 parent child 的 subagent member提供 session navigation。原生已有 `CoreWorkflowRunNode` keyed Chat snapshot，但尚未有 renderer。

## 参考

[1]: https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-subagent/src/client/index.ts "RC8 subagent slot registration"
[2]: https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-subagent/src/client/SubagentCatalogAction.tsx "RC8 subagent catalog"
[3]: https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-workflow-run/src/client/WorkflowRunPanel.tsx "RC8 WorkflowRunPanel"

## Workflow 初始原生实现

`NativeWorkflowRunPanel` 已接入 `NativeConversationNodeRow` 的 `CoreWorkflowRunNode` 分派。它显示 Core run/phase/member/status 的 typed snapshot，并使用登记的 official locale fallback；未从 raw events推断状态。初始范围不含 RC8 focus-aware disclosure、only-running-child navigation、snapshot/AX与 paired visual evidence，故不得作为 workflow sub项完成证据。

## 扩展面审计补充

本地 `feedback` 命中的是 `NativeApprovalQuestionViews` 的 question form validation feedback，并非独立 Host extension node。`modelDirectory` 目前由 `sessions.models` authority response 填充，检索未发现 conversation renderer consumer；permission 当前由 composer fixture/approval UI覆盖。下一步应继续以已有官方 node definition为入口，而不是为字符串命中自创独立 UI。

## Model retry

RC8 `MessageItem.tsx:55-112` 为 `model-retry` 仅显示当前 attempt：scheduled 时以 250ms timer刷新剩余整秒，其他状态显示原始 scheduled seconds；summary是 role=status，details显示 rounded delay ms与 failure message。Core `CoreRetryNode` 已包含 correlated attempts/current状态，下一 renderer应只消费该 typed snapshot。

## Model retry 初始原生实现

`CoreRetryAttempt` 现保存 RC8 `LlmRetryEventData` 的 `delayMs`、failure message、mode和maxRetries，started/cancelled状态转换保留这些 facts。`NativeModelRetryRow` 已作为 `model-retry` chat node renderer显示当前 attempt、状态摘要与 delay/failure disclosure，不读取 raw event。250ms live countdown、fixture/snapshot、AX及 paired visual evidence仍待闭环。

## Terminal turn error 初始原生实现

`NativeTurnErrorNotice` 已消费 `CoreTurnErrorNode` 的 title/message/optional code，按 RC8 `TurnErrorItem` 作为 error status row显示。retry-linked terminal errors仍由 Core `hiddenByRetry`/node visibility排除，native renderer不二次判断。fixture/snapshot、paired visual与完整 AX仍待闭环。

## 当前平台修复

macOS-26 run `32391234734` 显示 `NativeWorkflowRunPanel` 无法解析 `CoreWorkflowRunNode`；同类 retry/error renderer也直接引用 GlassCore types。原因是三个新 UI source只 testable 导入 GlassSpec。三者现均显式 `@testable import GlassCore`。Linux SwiftPM build仅在预期的 `OfficialThemeCatalog.swift: import AppKit` 平台边界停止，不能替代 macOS类型验证。

## Subagent catalog authority seam

已新增 `DSHAPIClient.subagentList`、`SubagentsAPI.list` 及完整 `SubagentListRequest/Response/EntryDTO`。它严格映射 RC8 `subagent.list({ parentSessionId }) -> { entries, parentAvailable }`，保留 child/diagnostic discriminator所需字段，暂不在 UI中以 summaries伪造 catalog。catalog header action、recursive disclosure、read-only composer、fixture/AX/paired visual仍待实现。

## Subagent catalog session state

`NativeSessionStore` 现持有 optional complete `subagentCatalog`、loading state与 explicit `refreshSubagentCatalog()`。刷新只使用 `NativeSubagentCatalogAPI`，并以 recovery generation和active session fence拒绝旧响应；switch/deinit取消并清空旧 catalog。`NativeShellPresentation.connectVerifiedHost` 将 `apis.subagents` 注入 store。该层尚未渲染 tree或接管 composer。

## Subagent catalog initial header renderer

`NativeSubagentCatalogHeaderAction` 已由 `NativeShellPresentation` 注册为 header actions order 10。它在出现时显式刷新 Host catalog，且仅在 complete catalog提供 child或 diagnostic evidence后显示 28px trigger；popover使用 RC8 336px width展示 direct child mode/activity与 diagnostics。当前不从 summaries推导 rows，也尚未实现递归 branch、child navigation、read-only composer、fixture/AX或 paired visual evidence。

## Subagent direct-child navigation

`NativeConversationContributionContext` 现携带 shell-owned `openSession` closure。首层 catalog healthy child rows以真实 native button调用该 closure，关闭 popover后打开对应 Host session；诊断仍不可操作。此处不生成 child session、未将 ordinary summaries当作 catalog，递归 branch与composer仍待实现。

## Subagent continuable actions

已增加 `SubagentPromptRequest` 与 `SubagentInterruptRequest`，均强制 durable `parentSessionId`、`childSessionId`与 `mode=continuable`，分别映射 RC8 `subagent.prompt` 与 `subagent.interrupt`。prompt receipt只保留 accepted messageId，interrupt receipt只表示 admission；二者均不乐观修改 Host child running/queue state。native child composer与 interrupt controls仍待实现。

## One-shot subagent read-only composer

`NativeSubagentReadOnlyComposer` 已映射 RC8 one-shot read-only title/body，并在 approval/question takeover之后、普通 composer之前，仅当 `extensionState.subagentIdentity` 是有效 `.identity(mode: .oneShot)` 时出现。absent、null sentinel与 malformed descriptor保持普通 composer，避免猜测 read-only；continuable parent-unavailable与typed child input仍待实现。

## Recursive subagent catalog foundation

`NativeSessionStore` 现保存按 parent ID 键控的 complete catalog cache、loading IDs与各 parent task；所有请求沿 selected-root generation fence执行。`NativeSubagentCatalogHeaderAction` 根据 `hasChildren` 展开/折叠，并仅调用 `refreshSubagentCatalog(parentSessionID:)` 后渲染该 parent 的 Host rows，不会从 summaries递归填充。递归菜单的完整 keyboard tree、loading/retry rows、paired visual/AX仍待闭环。

## Continuable subagent route 与 parent-unavailable takeover

RC8 `packages/client/runtime/src/client/sessions/session.ts` 将有效 continuable address 的 prompt/cancel 分别路由到 `subagent.prompt` / `subagent.interrupt`，不以普通 `session.prompt`/`session.cancel` 代替；`ui-subagent/src/client/index.ts` 则在 one-shot 或 parent 不可用时接管 composer 为只读。本地现以 `NativeSessionStore.SubagentRoute` 保留仅由 complete Host catalog child row给出的 `parentSessionID`、`childSessionID`、known mode及 `parentAvailable`。普通 session open 会丢弃不匹配 child 的旧 route，catalog child navigation 会保留匹配 route，因而不从 session summary或 ID 文本推断权限。

有效且 parent available 的 continuable route 只允许通过注入自同一 verified `HarnessAPIs` composition root 的 `SubagentsAPI` 发送 typed `SubagentPromptRequest`；running child 的取消只发送 typed `SubagentInterruptRequest`，即使 parent 当前不可用也不退化为普通 session cancel。one-shot、diagnostic、未知 mode或缺 continuation facade均 fail closed；`parentAvailable=false` 在 view层显示官方 read-only composer，阻止输入提交。`NativeSessionStoreTests.testContinuableSubagentRouteUsesParentChildPromptAndInterrupt` 锁定完整 parent-child request和普通 session facade零调用；`testSubagentRouteAcceptsOnlyCatalogChildWithKnownMode` 锁定 route筛选。仍须完成 composer互动视觉/AX、catalog loading/retry rows和 macOS-26 权威测试/official-native配对，故 T6.6 不勾选。

## 参考补充

[4]: https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/runtime/src/client/sessions/session.ts "RC8 continuable prompt and interrupt routing"
[5]: https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/runtime/src/client/sessions/manager.ts "RC8 direct-child catalog and selection authority"

## Catalog loading 与 retry rows

RC8 `SubagentCatalogAction.tsx` 将 catalog state 作为每个 parent 的事实：bare loading 不单独制造 trigger，但已有 child evidence或失败应保留入口；已展开 branch只展示该 parent 的加载或错误状态，并通过 `refresh(parentSessionId)` 重试。本地 `NativeSessionStore` 现发布 `loadingSubagentCatalogIDs` 与 `failedSubagentCatalogIDs`，二者都只含 direct parent ID、不含本地推导 entries；新请求先撤销该 parent 的旧失败标记，成功 complete snapshot清除失败，失败删除该 parent cache且仅发布 retry eligibility。`

`NativeSubagentCatalogHeaderAction` 使用登记的 `subagentLoading`、`subagentLoadError`、`subagentRetry` locale 显示 root 或已展开 branch的 ProgressView / error + retry row。Retry 只调用 `refreshSubagentCatalog(parentSessionID:)`，不保留或自造 descendant。Core 回归验证一次失败形成 root failed ID，下一次成功 Host snapshot清除它。keyboard tree和同状态 official/native视觉/AX证据仍待完成。

## Compaction transcript renderer

RC8 `ui-conversation/src/client/chat/CompactionItem.tsx` 只在 landed compaction checkpoint存在时显示 marker，不显示模型 checkpoint payload；有 `shadowedItemCount`/`shadowedTokenCount` 时使用 completed summary，缺 summary则禁用 disclosure并显示 unavailable，summary存在时才允许展开其安全 Markdown。原生 `NativeCompactionRow` 仅消费 `CoreCompactionNode` 的 checkpoint-gated typed facts，已接入 `NativeConversationNodeRow`，并复用 `NativeMarkdownText`；它不重写 shadowed history、不读 raw events。官方 locale来自 `ui-conversation/src/client/locales.ts:291-295`，回归验证这些运行态 locale及 completed count format。仍缺 fixture/native snapshot、macOS-26 XCTest、official/native visual pair与人工差异分类，故 T6.6 保持未勾选。

## Workflow disclosure 与 only-running-child navigation

RC8 `ui-workflow-run/src/client/WorkflowRunPanel.tsx` 的 `RunHeader` / `PhaseSection` 均为 status disclosure；`MemberRow` 仅当 injected navigable list包含该 member的 typed child ID时执行 `openSession`，否则保持非导航行（focused non-navigable仅为 web keyboard 呈现，不赋予点击）。本地 `NativeWorkflowRunPanel` 现在以 shell-owned closure接收导航 intent，run/phase建立本地 disclosure，running phase随 typed status展开。`NativeWorkflowRunPresentation.isNavigable` 只接受 `status == .running && childID非空`，绝不使用 label或 session summary推断；App 回归覆盖 running/finished/malformed child与 active phase keys。剩余差距是官方焦点细节、完整 status-count summary、paired visual/AX与人工差异分类，故 T6.6 不勾选。

## Trajectory toolbar 与 typed ledger基础

RC8 `ui-trajectory/src/client/locales.ts` 定义 `Trajectory toolbar`、`Turns`、`Calls`、`Search trajectory` 和 `Search`；`TrajectoryTable.tsx` 是完整 turn-aware ledger。原生当前不重放 raw history：Turns仅使用 reducer已 materialize 的 `target=trajectory` `CoreUserMessageNode`，Calls仅使用 Store已 materialize 的 `ToolInvocation(name, arguments, sequence)`。`NativeTrajectoryView` 增加官方 locale 的 segmented Turns/Calls control和搜索输入，搜索仅过滤上述 typed fields。完整官方 turn grouping/collapse、tool duration/status、selected record details、timing、虚拟化、paired visual/AX均仍待实现，T6.6保持未勾选。

## Compaction snapshot capture入口

`DSH_GLASS_SNAPSHOT_MODE=compaction` 现使用 `loadSnapshotCompactionFixture()`：先建立既有 Host-shaped tooling session，再追加与 `ConversationCoreNodesTests` 相同的 `compaction/start`、`compaction/summary` 与 landed checkpoint `user/message surfaceOp=replace` 形状。`SnapshotExporter` 以独立 conversation mode装配该 fixture；Core 回归断言 typed `CoreCompactionNode` 的 ID、summary、counts、checkpoint seq及非 running状态。这为后续 macOS官方/原生配对提供确定入口，不构成视觉完成证据。

## Trajectory selected Call inspector

RC8 `TrajectoryTable.tsx:894-913,2672` 的非 Markdown tool record详情提供 Event details tablist，Payload仅在 input detail存在时出现、Result仅在 output detail存在时出现。原生当前仅覆盖已 materialize `ToolInvocation`：Calls row selection受当前 typed filter约束；inspector无条件显示 Payload（arguments），且只在 output非空时显示 Result，切回 Turns或 output撤销时 fail closed回到安全状态。标题和 tab文本登记在 `OfficialUISpec.Text`；尚未实现其他 Core record kinds、schema/timing和官方视觉/AX比对。

## Subagent catalog keyboard tree

RC8 `ui-subagent` 的 `CatalogRows` 将每个 child作为单一 `treeitem`：Enter/Space打开 child，ArrowRight仅在未知叶子且未展开时展开，ArrowLeft仅在已展开时收起；鼠标 disclosure `tabIndex=-1`，因此不会制造第二个键盘停靠点。原生 child row保持 session Button的 Enter/Space，并用 `.onKeyPress(.rightArrow/.leftArrow)` 精确映射这两个 branch条件；鼠标 disclosure设为不可焦点且采用 `branch.expand/collapse` 官方 aria label。递归 Host catalog、loading/retry和导航 authority未改变。仍需 macOS AX树、键盘实际运行和 paired visual验收。

## Message feedback typed transport

RC8 `packages/feedback/message-feedback/src/types.ts` 定义 session/message scoped `list`、versioned `put`、versioned `delete`；`ifVersion: null` 在 put 中明确表示“观察到不存在”，而 `version-conflict` 会携带 authoritative `current` item。原生已新增 `MessageFeedbackAPI`，严格映射 `messageFeedback.list/put/delete` 的 result envelope；`MessageFeedbackPutRequest` 显式编码 `ifVersion: null`，不让 Codable省略该 precondition，且 business error保留为返回值而非 transport error。`MessageFeedbackTransportTests` 覆盖 null/version/conflict shapes。Store需继续实现 RC8 serialized mutation、list resync、conflict current reconciliation与 assistant-row UI；在这些 Host authority边界完整前不显示本地伪造 rating。

## Message feedback complete-list Store projection

原生 `NativeSessionStore` 现在通过 verified `HarnessAPIs.feedback` 接收 `NativeMessageFeedbackAPI`，并在每次 open 后进行 complete `messageFeedback.list` read。结果由 selected session 与 `recoveryGeneration` 双重 fence保护；switch、clear与disconnect取消 task并清空 sidecar，任何 business/carrier失败也只保留 retry事实而不显示旧 rating。`NativeSessionStoreTests` 覆盖 success complete snapshot和 rejected list的清空语义。此阶段仍没有本地 mutation或 assistant-row控件；必须先实现 RC8 serialized `put/delete`、version-conflict current reconciliation和缺失插件的无控件降级。

## Message feedback serialized mutation controller

RC8 controller将每项操作串到 session mutation tail，先保证 complete list已载入，再使用 committed item version执行 put/delete；成功只 commit Host returned item或 absent，`version-conflict` 只以 `error.current` 覆盖该 message，其他失败不写入。原生 Store相同地实现 `toggleMessageFeedback` 与 `clearMessageFeedback`，并以独立 mutation generation防止旧队列任务清理新任务的 busy状态。Core regression覆盖 v1 request、note继承、v2 Host success、v3 conflict current reconciliation。assistant-row UI、localized failure presentation、note editor和缺失插件无控件降级仍待实现。

## Native message feedback action row

RC8 `MessageFeedbackActions.tsx` 将 like/dislike只作为 per-assistant-message action；active rating改为 Remove rating label，点击交给 controller toggle，pending时禁用。原生 `NativeMessageFeedbackActions` 只在 settled `CoreAssistantNode.messageID` 非空时挂载，使用 Store typed projection和版本化 toggle，不从 node key、text或 raw history推断 feedback身份。当前覆盖 like/dislike；RC8 note editor、failure row以及完整 plugin-missing visual/AX证据仍待实现。

## Message feedback missing-plugin downgrade

feedback controls now additionally require `NativeSessionStore.isMessageFeedbackAvailable`, published only when a verified `MessageFeedbackAPI` is injected. Any absent/cleared/disconnected facade removes controls altogether; this prevents a UI action that would inevitably fail against a missing plugin. Core tests cover absent-to-injected availability. Note editing and localized row failure presentation remain separate work.

## Message feedback note mutation boundary

Store now admits `saveMessageFeedbackNote` only for an existing committed feedback item. It trims user draft, sends the current rating and committed version through the same serialized put path, and maps an empty draft to nil note so the Host performs the RC8 clear-note operation. Core coverage asserts rating, trimmed note and v1 condition. The native note popover and session-scoped failure presentation remain deliberately unmounted until their lifecycle can be matched to the controller.

## Native message feedback note popover

The native row now shows the note trigger only when a committed feedback item exists, matching RC8. It seeds the draft from the Host item and renders the official dialog, placeholder, save/cancel and accessibility labels. Save delegates to Store and retains the panel across failures; it closes only after a failure-free completion that replaced the Host item version. This maintains the controller's authority boundary pending final macOS visual/AX evidence and localized failure-row parity.

## Message feedback failure attribution

The Store publishes the message ID of the currently settled feedback mutation alongside its failure code. Native rows render an action failure only when that ID matches their typed assistant message; list failure maps to the official load label, version conflict maps to the official current-state label, and other failures map to the official generic label. The note panel retains its editing session across failure, matching RC8's correction path. Locale regressions assert every failure string is catalog-backed.

## Message feedback snapshot capture

`DSH_GLASS_SNAPSHOT_MODE=feedback` now constructs the tooling transcript through a Host-shaped assistant event plus `turn/end`, so the `CoreAssistantNode` becomes settled under the same reducer path as production. It publishes a capture-only complete feedback sidecar keyed by the assistant's typed message ID. Core regression checks settled status, key, rating and note. The fixture is solely a deterministic entry point for future macOS light/dark paired captures; it is not visual acceptance itself.

## Trajectory typed Call status

RC8 `TrajectoryTable.tsx:667-680` derives a record's state from typed cell facts and labels it `Pending`, `Completed`, or `Failed`. Native Call rows now render only the pre-materialized `ToolInvocation.state`: running maps to the RC8 Pending label, completed and failed map to the same RC8 labels, and the already-modelled stopped state retains the existing official tool-status label. No status is inferred from output content, timing, raw history, or sequence. The exact timing and turn grouping portions still require their own typed authority and remain out of scope for this change.

## Feedback note panel geometry correction

macOS build `32439862290` identified two unsupported feedback renderer tokens before test execution: a typo for the existing 13px medium typography role and an undeclared 280px width. Locked RC8 `ui-message-feedback/MessageFeedbackActions.module.css` defines `.notePanel` at `width: 320px` with `padding: 8px`; native now uses the existing `xsStrong13`, a sourced `Geometry.px320`, and the matching 8px outer padding. This establishes the exact source-backed geometry needed for later native/official paired visual validation, which is still not complete.

## Jobs action and StateDot specification convergence

RC8 `ui-jobs` registers its `JobListAction` as a `conversation.session.header.actions` contribution at order 20. Its only business input is the `jobsBySession` whole-list mirror; native continues to consume only `NativeSessionStore.backgroundJobs` and preserves the existing official ordering/elapsed-time presentation. The native header action now matches the locked `JobListAction.module.css` trigger and list geometry through `OfficialUISpec`, exposes the official `list.aria` label, and replaces approximate 6/7px circles with a reusable native StateDot.

The new StateDot mirrors `ui-primitives/StateDot`: settled/warning/error render a 10px same-color 10% halo with a 6px core; ongoing renders the RC8 8-cell, 2px-grid stepped chase using the generated `--dsw-static-deepseek-450` token. Its state is decorative and accessibility-hidden; the typed row status remains semantic. Official/native paired captures, timing-frame inspection, and macOS AX runtime evidence remain necessary before T6.6 can close.

## Feedback placeholder token correction

macOS build `32440254526` proceeded past the note-panel geometry fix and found the next unsupported token: the native placeholder overlay referenced an absent generic `Token.placeholder`. Locked RC8 `ui-primitives/Input.module.css` defines input placeholders with `--dsw-alias-label-dimmed`; `OfficialUISpec.Token.placeholder` now maps directly to the generated `aliasLabelDimmed` token. The feedback note editor keeps its existing source-backed placeholder text and uses that semantic official color rather than an invented fallback. This correction awaits a newer macOS authority run and paired visual evidence.

## Workflow focus-aware clean collapse

RC8 `WorkflowRunPanel.tsx:92-148,351-419` tracks each run and phase disclosure using typed mode (`clean`, `running`, `abnormal`), member activity count, open state, and `pendingCleanCollapse`. A workflow that turns clean stays expanded only while focus remains in its member content, then collapses on the focus boundary; fresh activity and abnormal escalation reopen it. Native `NativeWorkflowRunPresentation` now mirrors this as pure testable state transitions. `NativeWorkflowRunPanel` tracks typed phase/run disclosure state and only considers focus held by a real, navigable running child button; stale/malformed/non-navigable members cannot manufacture focus authority. App regression covers focus-held clean settlement, focus-leave collapse, active reopening, and abnormal escalation. Native focus ring geometry, keyboard session navigation, paired visual evidence, and macOS AX runtime validation remain pending.

## Feedback textarea inferred-height removal

macOS build `32440620940` progressed past the placeholder token and surfaced a third unsupported feedback reference: `Geometry.px72`. The locked RC8 `.noteInput` CSS specifies width, padding, border, radius, background, font and `resize: vertical`, but no minimum height. Native therefore removes the invented 72px constraint rather than registering a false official token; the TextEditor remains inside the documented 320px panel and is still sourced through the note input's official typography/color/border roles. The next macOS authority run must validate this compile repair and future paired capture must validate the system editor's height behavior.

## Model retry snapshot capture

`DSH_GLASS_SNAPSHOT_MODE=retry` now loads a snapshot-only Host-shaped `llm/retry` scheduled event after the normal tooling transcript. Its payload is the same typed shape verified in `ConversationCoreNodesTests` (`retryId`, retry/turn/step, mode, `maxRetries`, `delayMs`, and failure message); the Store lets the reducer materialize the `CoreRetryNode` and never injects a renderer-specific retry object. `NativeSessionStoreTests` asserts sequence, scheduled state, delay, failure, maximum, bounded retry mode, and running status. It is a deterministic light/dark native capture entry only; AX, paired official/native captures, and diff classification remain outstanding.

## Model retry scheduled countdown

RC8 `MessageItem.tsx:57-93` computes a scheduled attempt's deadline from its typed `delayMs`, samples an initial rounded-up seconds value, then refreshes it every 250ms only until the stable final one-second display. Native `NativeModelRetryRow` now follows that rule with local presentation-only deadline/countdown state. It resets only if the typed attempt changes, runs only for `.scheduled`, uses the existing official retry status template, and stops updating at one second. Started/cancelled attempts retain the durable scheduled-seconds projection; no timer result modifies Store, reducer, or Host authority. Deterministic scheduled fixture capture now enables visual review; AX and paired captures remain open.

## Model selection Host-confirmed transport

RC8 `packages/host/apiproxy/src/api/sessions.ts:291-302` defines `session.selectModel` with `sessionId`, provider, model and optional reasoning effort, returning a Host-validated `{ selected }` selection. Native now carries that exact request/response through `DSHAPIClient`, `SessionsAPI`, and the `NativeSessionAPI` seam; absent verified facades reject the mutation. The Store only submits routes contained in its complete `session.models` directory, fences the mutation by active session/recovery/mutation generations, and only replaces `current` after the Host response. Provider groups and failure rows stay immutable; a transport/business rejection keeps the last Host selection. Lockstep transport fixtures and Core tests cover optional effort wire shape, advertised-route gating, confirmed selection, and rejection behavior. The native composer control now consumes only a loaded routable directory, uses the RC8 root/model/effort menu structure and catalog locale, and is backed by a deterministic `DSH_GLASS_SNAPSHOT_MODE=model` whole-directory fixture plus Core regression. Host-missing/load failure presentation, macOS AX runtime and paired visual evidence remain pending.

## ModelSelect composer renderer facts

Locked RC8 `packages/client/ui-model-selection/src/client/ModelSelect.tsx:2-12,28-37,67-103,127-211,219-360` defines the model seat as a composer control, not a transcript node. Its trigger shows model plus caption-tone effort. The menu is two-level: root Model/Effort cells, a provider-grouped model radio list, and a reasoning-effort radio list; selected markers are trailing checks. It submits the complete directory selection only through `session.selectModel`, and rejected selection reports transiently rather than changing the catalog. `InputBar.tsx:491-511,695-720` establishes that the model seat's `locked` source is session removal (`modelSeatLocked`), not ordinary turn running; thus native must not disable it merely because the conversation is running. Locked `ModelSelect.module.css:99-270` specifies the status/error rows, grouped model rows, root-cell 40px geometry and primary/tertiary typography roles. This evidence now drives `NativeComposerModelSelector`: loaded routable directory only; trigger/model/effort labels and warning text resolve through the locked catalog; model/default-effort and effort-only choices delegate to Store's Host-confirmed action; catalog failure rows remain presentation-only. The root macOS menu nesting supplies native keyboard behavior. Host-missing/load failure presentation, AX runtime and paired visual classification remain open.

## Model selection deterministic capture

`DSH_GLASS_SNAPSHOT_MODE=model` now creates the `session.models` whole-snapshot shape used by the native composer: a current provider/model/balanced effort, two provider groups, explicit provider default and effort choices, plus a diagnostic catalog failure. It does not inject an accepted mutation or make a missing Host facade available. Core regression verifies all directory facts; it is capture scaffolding only, not paired visual evidence.

## Session permission projection and command seam

Locked RC8 `interaction/permission-presets/src/types.ts:12-43` declares `permissions` as an optional whole session projection with ordered `{ value, name, description? }` options and `currentValue`; key absence means the capability is not composed and the control must hide. `src/index.ts:227-275,324-337,370-390` confirms that `permission/preset`, `sandbox/mode`, and `approval/policy` are folded by the Host projection service, while the only current-session write path is the `/permission <preset>` command and its next pushed projection confirms visible state. Native now has a strict projection reader that rejects absent/null/malformed/duplicate/unknown-current values, exposes it only through `extensionState.permissions`, and sends only advertised non-`custom` selections through the typed session command seam. The Store fences tasks by session/recovery/mutation generations and never edits current/options optimistically. Core regression locks parsing, custom/unknown fail-closed routing, command shape, and projection-only confirmation. The composer renderer, full-access acknowledgement, snapshot capture, AX and paired visual evidence remain pending.


## Native session permission selector and capture

`NativeComposerPermissionSelector` now occupies the RC8 composer Access seat before Model. It renders only a valid session `permissions` projection, omits derived `custom` from switch targets, keeps the selected marker, and resolves `input.accessMode` plus risk-confirmation copy through the locked `ui-conversation` catalog. `danger-full-access` follows RC8 `PermissionSelect.tsx:60-62` with the versioned `Full access` display token and requires a native acknowledgement popover before delegation; all other advertised values delegate directly. The selection does not disable merely because a turn is running, and the Store's existing projection-only confirmation rule remains intact. `DSH_GLASS_SNAPSHOT_MODE=permission` injects an independent complete Host projection fixture; Core and App locale regressions cover the fixture and catalog lookup. The actual macOS menu/popover AX tree, full-access interaction, official/native paired visual capture, and manual difference classification remain open.


## Model directory lifecycle and reload

RC8 `ui-model-selection/directory.ts` makes the complete `session.models` response immutable authority and separately publishes `idle`, `loading`, `ready`, `selecting`, or `error` lifecycle state. The native Store now mirrors that distinction through `ModelDirectoryStatus` and one model-directory generation shared by initial open, authority recovery, reload, and selection. A late `models()` result cannot replace a newer reload or Host-confirmed `selectModel` response. Selection failure preserves the latest complete directory while publishing a presentation-only error; `NativeComposerModelSelector` keeps the fallback trigger visible during a loading/error state and renders only official `status.loading`, `error.action`, and `action.reload` catalog strings. Reload is read-only `session.models`; it does not replay history or invent a directory. Core tests cover ready after baseline/confirmed selection and error after rejected selection. macOS Menu/AX behavior, progress/error geometry, screenshot capture, and paired official/native review remain open.


## T6.7 recovery model baseline regression

RC8 runtime README states that reconnect rebuild uses the Host-reported selection/directory and must not replace a newer selection with an older response. The native recovery path already requests `models()` before a full history rebuild; the new gap-recovery regression changes the fake Host directory between the initial load and resync, then asserts the recovered provider/model and `.ready` lifecycle. This is a focused authority-baseline proof only; it does not close the broader disconnect-point/replay-equivalence matrix required by T6.7.


The T6.7 recovery regression also covers a non-cooperative pending `selectModel`: authority recovery cancels the task, advances selection and directory generations, immediately clears the local busy marker, and cannot accept the late Host-selected route after releasing the fake. This keeps recovery's newer complete models baseline authoritative without fabricating selection state.


MacOS native-ui run `32442222873` found the permission confirmation title referenced a nonexistent `mStrong16` member. The renderer now uses the existing, equivalent generated RC8 typography token `baseStrong16` (16px medium); no copy, geometry, or interaction behavior changed. The fix remains pending a newer macOS run and visual review.

## T6.7 projection recovery authority regression

Locked RC8 `packages/client/runtime/src/client/sessions/projection-store.ts:1-8,141-176` defines Host-computed whole values as `key → { value, seq }`. A history-tail baseline applies the higher-sequence-wins rule; a key omitted by the Host baseline is capability-absent at that cut and must clear unless a newer push frame already superseded it. Its reconnect truncation rule separately drops rows after the new Host durable watermark before the next baseline lands. Native `testGapRecoveryReplacesWholeProjectionBaselineFromLatestHostAuthority` and `testSubscriptionWatermarkRecoveryRebuildsProjectionBaselineFromHostAuthority` now exercise the two production recovery entries: a live event gap and a `session/subscribed` rollback. Both return a newer `SessionHistoryResponse.projections` baseline, assert the recovered Host key at sequence 2, and prove removal of the Host-omitted legacy key. This adds only authority replay proofs; the full disconnect-point/replay-equivalence matrix remains open.


## T6.7 gap recovery live-buffer stitch

Locked RC8 `packages/client/runtime/src/client/sessions/session.ts:650-727` requires a gap to enter `stitching`, buffer every `session/event` received while the history tail is in flight, install the recovered authority window, then append only buffered events whose sequence is strictly newer than the recovered tail. Replay overlap is therefore dropped by sequence, but post-cut events are retained. Its locked `session.client.spec.ts:690-800` also requires a failed repair to clear only the in-flight marker: the old window remains visible and the already-buffered frames must land on a later successful repull. Native `NativeSessionStore` now has an internal recovery-generation-bound `recoveryLiveBuffer`: the first gap frame and all subsequent event frames wait there; after the replacement history/projection/model baseline lands, they are sorted and replayed through the existing typed reducer/effect path only if beyond the recovered tail. A failed recovery exposes its existing presentation error but preserves buffer contents for a subsequent recovery; session switch, clear selection and disconnect cancel or clear the buffer boundary. `testGapRecoveryStitchesBufferedLiveEventsAfterLatestHostWindow` proves Host seq-2 wins over a buffered overlap while seq-3 and seq-4 survive in ordered transcript and typed Chat nodes. `testFailedGapRecoveryKeepsBufferedFramesForLaterSuccessfulRepair` proves a failed first pull leaves the rendered window untouched and a later recovery stitches both the retained and new frames. `testConcurrentGapFramesCoalesceIntoOneAuthorityRecovery` holds the first repair history request, delivers a second discontinuous live event, and proves only one repull occurs while both frames remain outside the rendered window; a failed request still leaves the existing baseline intact. Locked RC8 `session.client.spec.ts:830-869` further requires a recovery result superseded by a newer Host resync to be ignored; `testHostRestartRecoverySupersedesStaleGapRepairWithoutDroppingLiveBuffer` now starts a delayed gap repair, injects a lower `session/subscribed` watermark, and proves the newer Host history/models authority wins while the previously buffered post-cut frame is retained. The broader reconnect and current-status equivalence matrix remains open.


## T6.7 cold-to-live ahead-watermark recovery

Locked RC8 `packages/client/runtime/src/client/sessions/session.ts:623-637` loads the history tail and then immediately performs a second `history` read if `session/subscribed.lastSeq` is greater than that window tail; it does not wait to append a discontinuous live event. Native `applySubscription` now requests its fenced models/history authority recovery whenever the subscribed durable watermark differs from the installed history high watermark: lower covers Host rollback, higher covers a cold-to-live tail that arrived behind the subscription baseline. `testSubscriptionWatermarkAheadOfHistoryTailRecoversAuthorityWindow` proves a `lastSeq=2` subscription after a seq-1 history baseline installs the recovered seq-2 window and its current Host model directory. This still does not certify every reconnect interleaving or current-status replay path.


## Native model and permission accessibility triggers

`NativeAccessibilityRuntimeTests.testNativeModelAndPermissionSelectorsExportCurrentOfficialTriggerNames` now mounts independent Host-shaped model-directory and permissions fixtures in native SwiftUI selector controls, then walks the macOS accessibility tree. It requires exactly one current Model/Effort trigger label and one current Access-mode trigger label, both generated by the locked RC8 locale namespaces with the actual fixture values. The test complements the existing static locale catalog assertions; it still depends on macOS XCTest accessibility trust and therefore does not complete the required full-access popover interaction, paired visual capture, or manual difference classification.


## T6.7 recovery projection/live-frame ordering

The locked RC8 projection store applies both history baselines and `session/projection` pushes under one higher-sequence-wins rule; its `installWindow` sequence seeds the recovered baseline before stitching buffered conversation frames. Native `testGapRecoveryDoesNotRegressNewerLiveProjectionWithHistoryBaseline` holds a gap repair history at `asOfSeq=2`, delivers a live projection at seq 3 while recovery is in flight, and then releases the history response. The test proves the seq-3 Host push remains visible and its row sequence remains 3 rather than regressing to the older recovered baseline. This adds one projection/replay interleaving proof only; broader projection key and transient-status matrices remain open.


## T6.7 feedback sidecar reconnect resync

Locked RC8 `packages/client/ui-message-feedback/src/client/controller.ts:140-166` distinguishes an initial `refresh()` from reconnect `resync()`: resync runs behind the controller's serialized mutation tail so a list reply cannot resurrect a version that a queued mutation has already committed. Native recovery now queues `resyncMessageFeedbackAfterRecovery()` behind `messageFeedbackMutationTask` only after the fenced models/history/projections baseline has landed. Feedback mutation fencing uses a separate session lifecycle generation—not the recovery generation—so an admitted same-session compare-and-set write remains eligible to settle before the recovery resync; open, clear selection and disconnect cancel it and advance that session generation. The resync issues the existing typed complete-list read and adopts only Host items. `testAuthorityRecoveryResyncsMessageFeedbackFromLatestHostList` starts with a version-1 rating, changes the Host list to version 2, triggers a gap recovery, and proves the recovery performs a second session-scoped list read that publishes only the updated Host item. `testRecoveryFeedbackResyncWaitsForCommittedSameSessionMutation` holds a v1→v2 mutation at the Host, completes recovery while it is in flight, proves no resync list starts early, then releases the mutation and verifies the subsequent complete Host v3 list wins. This adds feedback sidecar recovery authority; subagent catalog revalidation and complete current-status equivalence remain open.


## RC8 subagent catalog lifecycle reconnaissance

Locked RC8 `packages/client/ui-subagent/src/client/SubagentCatalogAction.tsx:416-448` reads complete `subagentsByParent` snapshots as catalog authority, derives healthy direct-child rows only from that catalog, and may preserve a disabled loading presentation while ordinary session summaries arrive before a descriptor-backed catalog. This establishes that summaries do not substitute for catalog navigation authority. Locked `packages/client/runtime/src/client/sessions/manager.ts:902-908` then confirms reconnect behavior: `handleConnected()` refreshes the selected parent, the selected address parent, and every currently open catalog before session resync. Native authority recovery now re-reads the active root, every already Host-backed parent key in `subagentCatalogs`, and the parent of a selected subagent route; it does not create a descendant cache from returned row metadata. `testAuthorityRecoveryResyncsObservedSubagentCatalogsFromHost` proves root and an expanded child parent both replace their old complete snapshots on recovery, while a returned grandchild row does not manufacture a `subagentCatalogs` entry. `testAuthorityRecoveryResyncsSelectedSubagentParentCatalog` covers the RC8 `selectedAddress.parentSessionId` case: an active child’s verified parent catalog is refreshed even when no local disclosure cache exists yet. UI disclosure-state observation and full macOS popover interaction evidence remain open.


## T6.6 native full-access confirmation accessibility

The selector’s RC8 `danger-full-access` popover now renders the same extracted `NativeFullAccessPermissionConfirmation` view used by a direct macOS AX runtime regression. The extraction does not alter the selector’s state ownership: the acknowledgement binding, submission disable state, cancel reset, and Host command seam remain in `NativeComposerPermissionSelector`; the content retains the locked `ui-conversation` title, description, acknowledgement, cancel, and enable labels plus registered typography and geometry tokens. `testNativeFullAccessConfirmationExportsOfficialAccessibleControls` mounts the real content and requires its dialog title, acknowledgement Toggle, Cancel, and Enable labels in the AX tree. This still does not prove menu traversal, the in-window popover presentation transition, user click behavior under the macOS runner, paired visual capture, or human difference classification.


## T6.7 restart interaction reset

Locked RC8 `packages/client/runtime/src/client/sessions/manager.ts:882-899` clears generation-scoped pending interactions on disconnect and drops buffered `approval/requested` / `question/requested` frames, because a resolved interaction while disconnected has no compensating frame and only mux-open replay may establish a new live request. Native subscription handling already clears `pendingApproval`, `pendingQuestion`, and their submission flags before authority recovery. `testSubscriptionRestartClearsPendingInteractionsUntilHostReplay` now locks that safety boundary for a lower durable watermark: previously rendered approval/question controls disappear immediately and cannot remain actionable until the Host sends a new request. This does not yet enumerate all transport-disconnect timing permutations.


## macOS visual evidence pipeline status

The repository’s authoritative `native-ui` workflow builds and tests on macOS-26/Xcode 26, renders the native shell at a fixed widened display, compares locked official and native PNG pairs, and uploads official/native/diff artifacts for fourteen days (`.github/workflows/native-ui.yml:334-496`). It already verifies welcome, rail, jobs, tooling, workspace interaction, approval, and question scenes. The current T6.6 model/permission/feedback/workflow renderer entries expose deterministic snapshot modes, but the workflow does not yet capture matching official/native visual pairs for those specific states; therefore their TODO evidence boundary remains correctly open even when model/permission/full-access AX runtime tests are present.


The locked official capture fixture currently exercises welcome, rail, workspace management/search, tooling inspector, approval, question, and jobs only (`tools/reference-capture/capture-official-welcome.e2e.ts`). Correspondingly, `glass/scripts/verify-native-shell.sh` does not invoke the existing native `model`, `permission`, `feedback`, `retry`, `trajectory`, or workflow snapshot modes. A valid T6.6 paired-visual closure for those renderers therefore requires adding an official Host-backed interaction fixture and matching native invocation plus policy comparison; a native-only PNG would not satisfy the RC8 evidence boundary.


## T6.7 stale initial-open authority fence

Locked RC8 `session.client.spec.ts:814-828` requires a successful but stale `doOpen` history response to be dropped when a later resync has taken ownership. Native `open` carries equivalent recovery-generation, endpoint, active-session, and model-directory generation guards. `testNewEndpointOpenSupersedesStaleInitialHistoryAuthority` parks an initial history response after its old model baseline is available, opens the same session against a new endpoint whose Host supplies a fresh model/history window, then releases the old response. The visible window and current model remain the new endpoint’s authority. This tests endpoint supersession; broader stream connection and transport throw permutations remain open.


The locked model-selection component test provides a complete directory fixture with `current`, provider groups, reasoning efforts, routability and load/error behavior (`packages/client/ui-model-selection/tests/model-select.client.spec.tsx`). The locked permission row test confirms menu dismissal and the Full access acknowledgement-before-enable flow, but it exercises the global settings-plane `permission` namespace rather than the session-level `conversation.input.access` projection (`packages/client/ui-permission-presets/tests/permission-presets-row.client.spec.tsx`). These are useful behavioral provenance, not interchangeable official browser screenshot fixtures for the native session composer.


The locked RC8 `apps/web/tests/scaffold.ts` documents `launchWebScaffold({ replayFixture, replayProvidersOnly: true })`: it boots the real web composition and mounts the real provider/model catalog without consuming a model stream, specifically for UI scenarios that need model directory labels rendered (`LaunchOptions.replayProvidersOnly`, lines 198-216). This is the viable foundation for an official model-selector capture, provided a no-call replay session fixture is created and the browser is driven through the real composer menu; it does not by itself seed session-level permissions or feedback.
