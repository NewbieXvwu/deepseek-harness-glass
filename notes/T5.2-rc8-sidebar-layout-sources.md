# T5.2：RC8 三栏侧栏状态机来源与实现映射

**官方基线：** `deepseek-ai/deepseek-harness@141eb6fef83422698aef7a981029e843e8161534`（`dsh-v0.1.0-rc.8`）。本记录只覆盖本次补充的窄窗口侧栏状态机；它不是 T5.2 或 T7.4 的视觉验收结论。

## 官方契约

官方 `columns.ts` 将侧栏收缩 rail 固定为 **56px**，并定义 `SIDEBAR_AUTO_COLLAPSE = 1024`；三栏 solver 本身不含断点逻辑，而由 `AppFrame` 决定有效 sidebar preference。[1] 在 `AppFrame.tsx` 中，`viewport < SIDEBAR_AUTO_COLLAPSE` 时默认收缩；用户在窄窗口执行展开时，组件使用 `narrowExpanded` 覆盖默认收缩，并将原有宽窗口 preference 保持不变。[2]

官方 `stores.ts` 将这一语义明确为状态对 `narrow` / `narrowExpanded`：窄窗口下 `toggleSidebar` 只翻转 `narrowExpanded`，跨越断点时 `setNarrow` 清除该临时覆盖；宽窗口才实际写入 sidebar preference。[3]

| 官方状态/动作 | 原生映射 | 保持的可观察语义 |
|---|---|---|
| `narrow = false` | `NativeSidebarLayoutState.isNarrow = false` | 使用持久的宽窗口收缩偏好。 |
| `narrow = true` | `setNarrow(true)` | 默认渲染 56px rail，并清除上一次窄窗口临时覆盖。 |
| `narrowExpanded = true` | `setCollapsed(false)` 在窄窗口的映射 | 允许窄窗口暂时展开，但不改写宽窗口偏好或拖拽宽度。 |
| 断点返回宽窗口 | `setNarrow(false)` | 清除临时覆盖并恢复原宽窗口收缩偏好。 |

## 原生实现与回归

`glass/Sources/UI/Shell/NativeSplitContainer.swift` 现在以 `NativeSidebarLayoutState` 统一保存窄窗口和宽窗口语义。`NativeShellController.viewDidLayout` 与 `renderPresentation()` 均把当前视口宽度写入该状态机，随后使用其派生的 `isCollapsed` 配置 `NSSplitViewController` 的 sidebar item。`NativeSidebarView` 的展开/收起回调通过 `NativeShellPresentation.setSidebarCollapsed(_:)` 进入同一状态机，避免在窄窗口直接改写宽窗口 preference。

`glass/Tests/App/NativeSplitLayoutPolicyTests.swift` 新增两条回归：其一验证默认窄窗口收缩、手动展开、重返宽窗口后恢复未修改的宽窗口偏好；其二验证已手动收缩的宽窗口在窄窗口临时展开后，重返宽窗口仍保持收缩。

## 本地门禁与未完成证据

本次修改已通过 Swift 语法解析、`git diff --check`、可移植 `GlassPortableCore` CLI 回归、SwiftPM target-graph 正/负例门禁与 CI workflow topology gate。由于本地为 Ubuntu，AppKit/SwiftUI 的实际 `NSSplitViewController` 运行态、1023px 原生 rail、键盘焦点、Light/Dark 图像及官方/原生配对，仍必须由该代码提交自身的 `macos-26` 工作流提供。T5.2、T5.6 与 T7.4 因而保持未勾选。

## References

[1]: https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-layout/src/client/columns.ts "RC8 three-column constants and concession solver"
[2]: https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-layout/src/client/AppFrame.tsx "RC8 AppFrame breakpoint and rendered sidebar semantics"
[3]: https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-layout/src/client/stores.ts "RC8 layout store state transitions"

## 2026-08-20：Details 状态机补充

锁定 RC8 `packages/client/ui-layout/src/client/stores.ts` 的 `createLayoutStore()` 明确将 details 作为宽度偏好：`details = 0` 表示关闭，`setDetails` 只写入 `[DETAILS_MIN, DETAILS_MAX]` clamp 区间；`closeDetails()` 直接写入 `0`，而 `openDetails()` 只在当前关闭时恢复 `DETAILS_DEFAULT`，不会恢复上次的拖拽宽度。该 store 还确认 `setNarrow()` 仅在 breakpoint 实际改变时清除 `narrowExpanded`，相同 narrow 值的重复 refresh 必须保持人工展开。

因此后续原生回归应分别验证 details 的 `open → close → default reopen` 语义，以及重复窄视口刷新不清除临时 rail 展开；两者不得被误写为宽窗口用户偏好的保持规则。

### Solver 与显式关闭的区别

`columns.ts:62–76` 还定义了不可混淆的 concession 行为：当视口不足以同时容纳 sidebar、details 和 `CENTER_MIN` 时，solver 可**派生** `details = 0`，但不会改写 store preference，因此窗口重新变宽会恢复原宽度。与之不同，用户的显式 `closeDetails()` 才将 preference 写为零，随后 `openDetails()` 恢复 `DETAILS_DEFAULT`。原生实现必须保持这两个路径分离：布局计算不足只临时折叠，关闭按钮才遗忘拖拽宽度。

### Session 切换与详情 subtree

RC8 `AppFrame.tsx:94–108` 在 current details session 从一个有效 session 切换到另一个有效 session 时调用 `actions.closeDetails()`；`DetailsColumn` 仍固定渲染于 column tree（`AppFrame.tsx:184–192`），宽度为零只负责视觉折叠，不以 unmount 取代状态收敛。原生端后续应单独验证 session 切换触发显式 details close（并按 `closeDetails()` 规则清零 preference），同时避免把 viewport concession 的临时折叠误当作显式关闭。
