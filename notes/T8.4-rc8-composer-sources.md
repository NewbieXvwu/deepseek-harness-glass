# T8.4：RC8 Composer 来源映射（进行中）

**锁定基线：** `deepseek-ai/deepseek-harness@141eb6fef83422698aef7a981029e843e8161534`（`dsh-v0.1.0-rc.8`）。官方来源为 [`packages/client/ui-conversation/src/client/skeleton/InputBar.tsx`](https://github.com/deepseek-ai/deepseek-harness/blob/141eb6fef83422698aef7a981029e843e8161534/packages/client/ui-conversation/src/client/skeleton/InputBar.tsx)。

| 官方位置 | 可观察语义 | 原生实施约束 |
|---|---|---|
| `InputBar.tsx:39-69` | composer 从 Host/machine 获取 draft、attachments、running、subagent、removed、blocked 与 projection；`empty` 同时考虑文本和附件。 | NativeSessionStore/Host 是唯一真源；不得依靠 UI optimistic transcript 伪造 durable message。 |
| `InputBar.tsx:120-144` | disabled 包括 removed、no workspace、machine 缺失、blocked、continuable child parent offline；running 仍可编辑；no-workspace textarea 是可访问的 workspace picker trigger，不应直接永久 disabled。 | 原生 composer 需区分 idle/running/no-workspace/blocked，不得以单一 disabled 状态压平。 |
| `InputBar.tsx:83-94` | Host prompt failure 显示 transient error，但 draft 保留以便重试。 | NativeSessionStore 已具备拒绝后不清空 draft 的 typed facade 运行态回归；T8.4 要实现用户可见 error surface。 |
| `InputBar.tsx:98-114, 280-353` | IME composition Enter 不提交；Shift+Enter 无条件换行；Escape/箭头有 machine arbitration；Cmd/Ctrl+Enter 走加速操作；普通 Enter 需 prevented、去重 repeat、在 locked/busy 状态不发送。 | SwiftUI/TextKit 实现必须覆盖 IME、Shift+Enter、Enter、Cmd+Enter 与 Stop 键盘路径，测试通过真实 responder/action 或可观察 facade 完成。 |
| `InputBar.tsx:136-143, 350-360` | 空 workspace 可触发恢复；empty accelerated Enter 可 steer queue（仅 running ordinary session，且 queue 有 queued row）。 | T8.4/T8.5 需以 Host queue 作为真源；未实现前不可伪造 steer 成功。 |

本记录仅覆盖当前已读取的 InputBar 段落。仍需读取其 submit/stop/render segment、CSS geometry、官方场景以及本仓库现有 `NativeInteractiveComposerCard` 后，才能实现或勾选 T8.4。

当前补充：`NativeSessionStoreTests.testCancelIntentUsesTypedFacadeOnlyForHostRunningTurn` 使用拒绝型 `NativeSessionAPI` recorder 运行时 replay `turn/start`，断言 idle composer 不制造 cancel RPC，running 状态才经 typed facade 发送 cancel，且 carrier 拒绝/返回不会乐观把 `isRunning` 置为 false。该证据落实 RC8 Host-authoritative stop 边界；完整 Composer 键盘/queue/blocked/accessory/视觉实现仍待继续。

补充 prompt 成功路径：`NativeSessionStoreTests.testAcceptedPromptClearsDraftOnlyAfterTypedHostFacadeAcceptance` 以返回 `SessionPromptResponse(accepted: true)` 的 typed facade 验证 draft 在调用后、Host acceptance 前仍保留，并仅在 acceptance 后被清空。该正例与拒绝后保留 draft 的回归共同覆盖官方 retry posture；永久 user row 仍必须等待 SSE durable event，故 T8.5 继续未完成。
