# T7.1：RC8 Sidebar 静态壳层官方来源审计

**官方基线：** `deepseek-ai/deepseek-harness@141eb6fef83422698aef7a981029e843e8161534`（`dsh-v0.1.0-rc.8`）。

本审计记录T7.1的可回链规格，不能替代同状态官方/原生截图、人工局部差异复核和当前HEAD macOS CI。T7.1在这些证据闭环前保持未勾选。

| 主题 | RC8官方来源 | 严格合同 | 原生实施约束 |
|---|---|---|---|
| Shell责任边界 | `packages/client/ui-sidebar/src/client/contract/slots.ts:2-8, 30-46, 62-101` | 壳层只拥有column geometry、brand row与New Session；`sidebar.workspaces`拥有浏览区域；`sidebar.settings`拥有固定foot的设置触发器；`sidebar.footer.action`为可选列表。 | SwiftUI shell不得将workspace/session或settings业务状态塞入静态壳层；必须分别装配workspace seat和settings seat。 |
| Wide静态结构 | `SidebarRoot.tsx:128-207` | wide时依序为可作为New Session捷径的brand、fold toggle、New Session capsule、workspace region、footer actions、settings。 | wide视觉树必须有两个通往同一New Session行为的可访问入口；无workspace时仍保留shell文案/结构，不用自创welcome替换。 |
| Rail结构 | `SidebarRoot.tsx:159-188, 190-207` | rail静态状态从上到下是brand/expand、New Session、workspace rail region、底部footer/settings；冷启动collapsed不能出现延迟隐藏图标。 | T7.4需要将wide/rail转场与reduced-motion完整对齐；T7.1保留四个seat和bottom settings的结构。 |
| Scroll与foot | `SidebarRoot.module.css:279-327` | region `flex:1; min-height:0; overflow:hidden`，恰好填充上部控制与foot之间；foot `flex:none`固定在底部。 | 原生workspace seat必须以可伸缩滚动区域占用剩余高度，settings被压到底部而非跟随短列表上移。 |
| Wide几何 | `SidebarRoot.module.css:9-30, 87-108, 227-247` | root padding `6px 12px`; logo row 60px/下8px; New Session 38px、高度和边框/12px radius、左右2px/下8px。 | 使用`OfficialUISpec`语义token/geometry映射，不在视图中新增私有产品文案或颜色。 |
| Brand | `SidebarRoot.tsx:128-155` 与 `SidebarRoot.module.css:110-160, 213-225` | 未填slot时显示fish mark、`DSH Local Build`和可选build hash；brand click等同New Session。 | 原生fallback必须只用Generated OfficialUI spec 文案与官方资产；hash可见性取决于构建事实，不伪造revision。 |
| Shell行为 | `packages/client/ui-sidebar/tests/sidebar-root.client.spec.tsx:82-141` | wide brand和New Session均触发`startSession`; panel触发toggle; region/settings/footer分别收到wide；workspace region跨collapse保持装配并在settle后切rail；rail可请求expand。 | 原生回归需要用实际交互closure、状态和SwiftUI/AppKit树验证，而不是搜源码关键词。 |

## 当前原生盘点（非完成声明）

`SidebarView.swift`已有官方fish mark、fallback brand、New Session、workspace browser和settings row。需要进一步验证并收敛：workspace seat在所有空/短列表状态都占用剩余高度、settings在foot正确固定、wide/rail两种状态的同RC8 viewport截图、以及静态shell的current-head CI。`NativeShellPresentation.sidebar`目前传入settings callback的最终settings root仍属T10范围，不能为了T7.1虚构设置面板。

## 后续验收

1. 使用RC8 official capture补充wide no-workspace / wide with-workspace / rail三种同尺寸浅深色截图。
2. 原生快照分别验证brand、New Session、workspace region、底部settings的坐标、可见性和可访问名称。
3. 对比New Session capsule边框、logo row高度、rail 36px controls、region底部与settings垂直锚定；记录所有局部差异和修复commit。
4. 在当前HEAD macOS-26 CI成功后，才更新TODO勾选和visual-review证据。
