# T6.6：扩展 Conversation Nodes 进度记录

**锁定官方版本：** `deepseek-ai/deepseek-harness@99f6f02fecdb7dff40c3fbc9470f5907c29f74ca`

## 已完成子块：durable inbox 与 steering 分类

官方 [`conversation-nodes/inbox.ts`][1] 把每个 `agent/inbox/spliced` durable event 作为 state-only context：`next-turn` 与 `next-step` 各自累计 pending identity；对于未取消的 next-step splice，被移除的 ID 进入 claimed 集合。随后 [`conversation-nodes/message.ts`][2] 只在 `user/message` 为 append surface、source kind 为 user 且该 message ID 已被 next-step claimed 时，将该消息 materialize 为 `steering`；否则它是普通 `user`，非 user source 则保持 `context`。

本地 `InboxDefinition` 以两个 Core-only state-only definition（`inbox-next-turn`、`inbox-next-step`）映射此语义，读取 `target`、`start`、`removedCount`、`inserted` 与 `outcome`，并通过 `ConversationContextReader.previous` 维护先前 durable inbox state。`InputMessageDefinition` 不再基于 UI 时间或 queue snapshot 猜测 steering，而是只消费该前置 Core state；`CoreUserMessageNode.Kind` 和 materialized chat target 现在明确包含 `steering`。

`testDurableNextStepInboxSpliceClassifiesOnlyClaimedUserMessageAsSteering` 构造一个完整的 next-step admission：先插入 `steer-me`，再用未取消 splice 移除，再 append 两个 user messages。断言被 Host 认领的第一条为 `steering`，未认领的第二条仍为 `user`，并且 inbox contexts 不生成虚假的 timeline/chat row。

## 当前验证状态

本地 D0 native-only、D1 official specification 和模块边界门禁已通过。Linux 环境没有 Swift/Xcode，新增 Swift XCTest 必须由本子块提交自身的 macOS-26 CI 验证；通过前 **T6.6 仍保持未勾选**。

## 明确未完成范围

T6.6 仍须完成并单独验证：todo/goal，queue snapshot 与 steering history 显示，approval/question durable interaction node，workflow/subagent/trajectory/deliverables/feedback，model/permission 状态与 jobs。每个家族都需要锁定官方事件来源、Host fixture、错误/取消路径、Core/renderer snapshot，并在其可见 renderer 到位后完成对应官方/原生视觉配对。当前子块不表示这些范围已经完成，也不替代 T6.7 reconnect/replay。

## References

[1]: https://github.com/deepseek-ai/deepseek-harness/blob/99f6f02fecdb7dff40c3fbc9470f5907c29f74ca/packages/client/ui-conversation/src/client/conversation-nodes/inbox.ts "官方 durable inbox node"
[2]: https://github.com/deepseek-ai/deepseek-harness/blob/99f6f02fecdb7dff40c3fbc9470f5907c29f74ca/packages/client/ui-conversation/src/client/conversation-nodes/message.ts "官方 user/steering/context message classification"

## CI 失败关闭记录

提交 `64f1018` 的 macOS-26 [run 32269297701](https://github.com/NewbieXvwu/deepseek-harness-glass/actions/runs/32269297701) 在独立 SwiftPM 编译阶段失败。原因是注册表调用 `InboxDefinition(target:)`，而结构体合成初始化器的字段标签为 `inboxTarget:`；未执行 XCTest 或视觉比较。后续修复仅将两个注册点更正为 `InboxDefinition(inboxTarget:)`，并必须由其自身的 macOS-26 run 重新验证。T6.6 继续保持未勾选。

## inbox/steering 子块验证结果

修复提交 `826b821` 的 macOS-26 [run 32269639042](https://github.com/NewbieXvwu/deepseek-harness-glass/actions/runs/32269639042) 已成功完成官方规格与原生架构门禁、独立 SwiftPM 编译、全量 XCTest、原生 app 装配、快照与官方视觉比较。该成功证明 `InboxDefinition` 的注册、state-only context、previous-context claimed lookup 和 InputMessage steering classification 可在 Apple Silicon Swift 6 上编译并通过全量回归。

此 run 仅为 inbox/steering **子块**的通过证据。T6.6 仍须在 todo/goal、queue、approval/question、workflow/subagent/trajectory/deliverables/feedback、model/permission 与 jobs 各自具有官方来源、fixture、取消/错误路径、snapshot 和独立 CI 证据后才能勾选。

## 进行中子块：goal whole-value projection

官方 [`goals.schema.ts`][3] 明确指出 mutation 只返回 `{ ref }` acknowledgement，当前状态**只**经 session projection key `goal` 传输。官方 [`goal/types.ts`][4] 将它定义为完整 `GoalProjection | null`：current durable `goal`（id、revision、objective、phase、blockedReason、maxGoalRounds）加 roundsStarted/createdAt/updatedAt；`null` 是 create 前或 clear 后 tombstone，whole-value last-wins 的折叠由 Host/runtime projection 层完成。

本地新增 `CoreGoalProjection` 与 `SessionGoalProjectionReader`。它只从 `SessionProjectionStore` 读取 `goal` 行，严格验证正整数 revision/maxGoalRounds、phase/blockedReason 一致性、非空 objective 与时间单调性；absent、tombstone 或 malformed Host value 一律返回安全的 `nil`，不会从 mutation 请求、本地草稿或事件重放猜测 goal。新增 `SessionProjectionStoreTests` 覆盖 higher-seq-wins、blocked 值、tombstone 与不一致值拒绝。

本子块待其自身的 macOS-26 CI 通过后才可标为已验证；它不等同于 T6.6 完成，也不替代 goal 的实际 native renderer/交互、todo/queue 或其余扩展节点。

[3]: https://github.com/deepseek-ai/deepseek-harness/blob/99f6f02fecdb7dff40c3fbc9470f5907c29f74ca/packages/host/apiproxy/src/api/goals.schema.ts "官方 goal mutation 和 projection 边界"
[4]: https://github.com/deepseek-ai/deepseek-harness/blob/99f6f02fecdb7dff40c3fbc9470f5907c29f74ca/packages/goal/goal/src/types.ts "官方 goal projection 类型"

## goal projection 子块验证结果

提交 `a925629` 的 macOS-26 [run 32271007659](https://github.com/NewbieXvwu/deepseek-harness-glass/actions/runs/32271007659) 成功完成官方规格与原生架构门禁、独立 SwiftPM 编译、全量 XCTest、原生 app 装配、快照与官方视觉比较。该成功验证了 `CoreGoalProjection` 的 strict whole-value decode、SessionProjectionStore 的 higher-seq authority、blocked/tombstone safe degradation 和全部既有回归。

这同样只是 T6.6 的一个子块；todo、queue/steering UI、approval/question durable node、workflow/subagent/trajectory/deliverables/feedback、model/permission 与 jobs 仍未完成，T6.6 保持未勾选。

## 进行中子块：todos whole-list projection

官方 [`TodoPanel.tsx`][5] 仅消费 dock adapter 选出的 `todos` plain list，空数组不渲染；官方 [`tool-todo/types.ts`][6] 声明该 projection 是最新 `todo/write` 的完整 replacement list 或 pre-first-write tombstone `null`，客户端不得做增量合并。

本地新增 `CoreTodoItem` 与 `SessionTodoProjectionReader`。它从 `SessionProjectionStore` 读取 `todos` whole-value，严格验证非空 content、闭合 status（pending/in_progress/completed）和 content identity 唯一性；`null`/缺失与 malformed 行安全降级为 `nil`，空数组保留为有效空计划。新增 projection store 回归覆盖 higher-seq list、空列表、重复内容拒绝与 tombstone。

本子块尚待本提交自身的 macOS-26 CI；即便通过，也不代表 TodoPanel renderer、dock snapshot 或 T6.6 其余节点已经完成。

[5]: https://github.com/deepseek-ai/deepseek-harness/blob/99f6f02fecdb7dff40c3fbc9470f5907c29f74ca/packages/client/ui-conversation/src/client/skeleton/TodoPanel.tsx "官方 TodoPanel 投影消费方式"
[6]: https://github.com/deepseek-ai/deepseek-harness/blob/99f6f02fecdb7dff40c3fbc9470f5907c29f74ca/packages/todo/tool-todo/src/types.ts "官方 todos whole-list projection 类型"

## todos projection 子块验证结果

提交 `c485064` 的 macOS-26 [run 32271891143](https://github.com/NewbieXvwu/deepseek-harness-glass/actions/runs/32271891143) 成功完成官方规格与原生架构门禁、独立 SwiftPM 编译、全量 XCTest、原生 app 装配、快照与官方视觉比较。该成功验证了 `SessionTodoProjectionReader` 对 Host whole-list 的 higher-seq authority、empty-list distinction、status/content validation、tombstone 与 malformed safe degradation。

此子块不闭合 T6.6。仍须实现并验证 queue/steering UI、approval/question durable interaction state、workflow/subagent/trajectory/deliverables/feedback、model/permission 与 jobs，以及它们各自的 renderer/错误/取消路径和视觉验收。

## 进行中子块：transient queue 与 durable steering handoff

官方 [`queue-mirror.ts`][7] 规定 `session/queue` 是完整的 transient Host snapshot；当连续 durable `user/message` 的 ID 与 transient steering row 的 `messageId` 匹配时，镜像必须只 retire 该 steering row，其他 queued/context 项保持不变。queue 不得由 durable history 反推，generation reset 必须清空旧 snapshot。

本地 `NativeSessionStore.apply(event:)` 现于处理 `user/message` 前按 message ID 移除对应 `.steering` queued row；新增 `NativeSessionStoreTests.testDurableUserMessageRetiresOnlyMatchingTransientSteeringRow` 以实际 `session/queue` 与 `session/event` RPC envelope 验证只移除 matching row、保留 ordinary queue、并正常 materialize durable transcript。

本子块尚待本次提交自身的 macOS-26 CI。它不替代 queue renderer、jobs presentation 或 T6.6 其余 interaction/plugin node 家族。

[7]: https://github.com/deepseek-ai/deepseek-harness/blob/99f6f02fecdb7dff40c3fbc9470f5907c29f74ca/packages/client/runtime/src/client/sessions/queue-mirror.ts "官方 transient queue 与 durable steering handoff"

## queue/steering handoff 子块验证结果

提交 `79e05ae` 的 macOS-26 [run 32272593313](https://github.com/NewbieXvwu/deepseek-harness-glass/actions/runs/32272593313) 成功完成官方规格与原生架构门禁、独立 SwiftPM 编译、全量 XCTest、原生 app 装配、快照与官方视觉比较。该成功验证了 `NativeSessionStore` 对 matching durable user message 的 steering-only retire，不影响其他 transient queue rows，并保留原有队列/作业完整 snapshot 与 generation reset 规则。

T6.6 仍未完成：approval/question durable interaction、workflow/subagent/trajectory/deliverables/feedback、model/permission、jobs presentation 以及每个可见 surface 的正式 renderer/视觉验收尚待实施。

## 后续子块来源：approval/question pending lifecycle

官方 [`sessions/pending.ts`][8] 将 approval/question 定义为 Host-owned `PendingWait`：render identity 是稳定的 `<prefix>:<rpcId>`，请求 payload 原样保留；raw rpcId 私有，只由 `respond()` 回填 client-response envelope；交互是否结算只由 pending-list membership/收到 resolved frame 决定，不能由按钮点击或本地 settled UI 猜测。这与本地 `NativeSessionStore.PendingApproval`、`PendingQuestion` 的 sessionID/rpcID 相关性和 `approval/resolved`/`question/resolved` 生命周期直接相关。

[8]: https://github.com/deepseek-ai/deepseek-harness/blob/99f6f02fecdb7dff40c3fbc9470f5907c29f74ca/packages/client/runtime/src/client/sessions/pending.ts "官方 pending approval/question lifecycle"

## 进行中子块：approval/question Host identity

新增 `testPendingApprovalAndQuestionClearOnlyOnMatchingHostResolution`：以真实 server-request envelopes 验证 pending approval 仅由相同 `approvalId` 的 resolved 清除，pending question 仅由相同 `questionRpcId` 的 resolved 清除；不匹配 resolution 不得误解除 Host-owned blocking state。这补齐既有实现的 RPC identity 回归，仍待本提交自身 macOS-26 CI。

## approval/question identity 子块验证结果

提交 `05fd978` 的 macOS-26 [run 32273507274](https://github.com/NewbieXvwu/deepseek-harness-glass/actions/runs/32273507274) 成功完成官方规格与原生架构门禁、独立 SwiftPM 编译、全量 XCTest、原生 app 装配、快照与官方视觉比较。该成功验证 approval/question pending state 只在相符的 Host resolved identity 抵达后清除，误配 resolution 不会解除阻塞表面。

T6.6 的未完成范围不变：workflow/subagent/trajectory/deliverables/feedback、model/permission、jobs presentation，以及各可见 surface 的 renderer、取消/错误路径和正式视觉验收仍需逐项完成。

## 剩余扩展节点的锁定来源入口

后续实现将分别以官方 `ui-workflow-run`、`ui-subagent`、`ui-trajectory`、`ui-deliverables`、`ui-message-feedback`、`ui-permission-presets` 与 `ui-jobs` 包为行为/视觉来源；session transport 则以 runtime `sessions/manager.ts` 与 `sessions/service.ts` 的 `session/jobs` whole-snapshot 语义为准。不得把这些插件面板降格为匿名 JSON 卡片，或把 plugin-defined payload 猜测为固定原生 schema；每一类必须分别建立 Host fixture、typed safe adapter、取消/错误路径、renderer snapshot 与同状态官方/原生视觉配对。

## 下一子块规格：jobs presentation

官方 `JobListAction.tsx` 规定：无 jobs 时 header control 不渲染；running/stopping 为 live；live jobs 先按 startedAt 升序，settled jobs 再按 finishedAt（回退 startedAt）降序；状态颜色语义为 running=ongoing、stopping/killed=warning、completed=done、failed=error；live duration 仅在展开面板时按秒刷新。既有 `NativeSessionStore.BackgroundJob` 已保留稳定 id/kind/label/status/detail/start/finish 与 `isLive`，下一实现将把该 Host whole snapshot 接入同等的原生 header/panel，而不会重折叠或猜测 job payload。

本地接入审阅：`NativeActiveConversationSurface` 目前以无状态 `NativeConversationHeader()` 作为 transcript 之上的唯一 header；它尚未接收 `NativeSessionStore`，且 UI 目录未发现可复用的 jobs/status popover。jobs presentation 将先把 header 改为显式接收 session store，再以 `BackgroundJob` typed snapshot 实现官方空态隐藏、排序、状态映射、展开态 live duration 和 accessibility，然后补齐官方/原生视觉配对；在此之前不得声称 jobs renderer 完成。
Jobs locale 审阅：锁定 `OfficialLocaleCatalog` 已含 `ui-jobs` count/status/duration 文案，但 `OfficialUISpec.Text` 尚未生成对应 typed Swift 常量。原生 jobs renderer 实施前需先按既有官方 locale 生成/查询路径补齐该受控入口并为 en/zh snapshot 覆盖；不得改用硬编码英文文案。

## 进行中子块：jobs presentation behavior

新增 `SessionJobsPresentation`，以官方 Jobs action 的规则提供 live-first/start-order、settled newest-first/fallback start-order 与 live/settled elapsed 计算；`NativeSessionStoreTests` 现覆盖 running/stopping、completed/failed 混排和 elapsed。该纯行为层待本提交的 macOS-26 CI，通过后仍需完成受控 locale、header/panel、live timer、accessibility 与视觉配对，不能将 jobs renderer 提前标记完成。

## jobs behavior 子块验证结果

提交 `9239e74` 的 macOS-26 [run 32274602096](https://github.com/NewbieXvwu/deepseek-harness-glass/actions/runs/32274602096) 成功完成官方规格与原生架构门禁、独立 SwiftPM 编译、全量 XCTest、原生 app 装配、快照与官方视觉比较。该成功验证 Host job whole snapshot 的官方排序和 elapsed 纯行为层。Jobs header/panel、受控 locale、live timer、accessibility 与视觉配对仍未完成，因此 T6.6 继续保持未勾选。

## 进行中子块：native Jobs header/panel

已将 `NativeJobsHeaderAction` 接入 `NativeConversationHeader`：仅 jobs 非空时出现；以 `SessionJobsPresentation` 排序/elapsed、`OfficialLocaleCatalog` 的 `ui-jobs` en/zh 文案和 Host `BackgroundJob` snapshot 生成 popover；live jobs 打开时每秒刷新，状态颜色遵循官方 running/stopping/killed/completed/failed 分类。此实现尚待本提交 macOS-26 编译、XCTest、原生截图与官方同状态视觉配对；通过前绝不将 jobs renderer 或 T6.6 勾选。


### Jobs renderer CI failure and correction

提交 `3a8ab37` 的 macOS-26 [run 32275389176](https://github.com/NewbieXvwu/deepseek-harness-glass/actions/runs/32275389176) 在 `Independently compile declared SwiftPM targets` 阶段失败：`NativeJobsHeaderAction` 使用的 `OfficialUISpec.Token.success` 尚未暴露，因此该 run **未**执行 XCTest、装配或视觉比较，不能作为通过证据。

纠正依据来自锁定官方 [`JobListAction.tsx`][9] 与 [`StateDot.module.css`][10]：`completed` 映射到 `StateDot("done")`，而 done 的前景色是 `--dsw-alias-state-success-primary`。本地生成的 `OfficialThemeCatalog` 已持有 `Theme.aliasStateSuccessPrimary`，修复仅补充保持 provenance 的 `OfficialUISpec.Token.success` native alias，绝不以中性或近似色替换。必须由随后的独立 corrective commit macOS-26 run 验证后，才可恢复 Jobs renderer 的候选状态。

[9]: https://github.com/deepseek-ai/deepseek-harness/blob/99f6f02fecdb7dff40c3fbc9470f5907c29f74ca/packages/client/ui-jobs/src/client/JobListAction.tsx "官方 Jobs status→StateDot 映射"
[10]: https://github.com/deepseek-ai/deepseek-harness/blob/99f6f02fecdb7dff40c3fbc9470f5907c29f74ca/packages/client/ui-primitives/src/StateDot.module.css "官方 StateDot done 成功态 token"


提交 `2d65893` 的 macOS-26 [run 32275984865](https://github.com/NewbieXvwu/deepseek-harness-glass/actions/runs/32275984865) 同样在独立 SwiftPM 编译阶段失败：completed 状态的 success alias 已修复，但 failed 分支仍错误引用不存在的 `OfficialUISpec.Token.error`。官方 `StateDot(error)` 对应的既有原生受控别名是 `OfficialUISpec.Token.errorPrimary`（`Theme.aliasStateErrorPrimary`）；后续修复仅替换此名称，并再次由新提交独立验证。该失败 run 未执行 XCTest、装配或视觉比较，仍不能用于任何通过声明。


## Jobs header/panel 编译与回归验证结果

纠正提交 `bc0588e` 的 macOS-26 [run 32276290443](https://github.com/NewbieXvwu/deepseek-harness-glass/actions/runs/32276290443) 已成功完成官方规格门禁、原生架构门禁、独立 SwiftPM 编译、全量 XCTest、原生应用装配、快照生成与现有官方 welcome 视觉比较。该 run 是 Jobs 原生 header/panel 行为及其完整既有回归的有效 Apple Silicon 验证证据；此前 `3a8ab37` 和 `2d65893` 的编译失败均已闭环，且未被误用为通过证据。

不过，当前视觉工作流只对 `welcome-no-workspace-light` 执行官方/原生配对，尚未捕获 Jobs 非空、展开且含 live/settled 状态的同状态官方/原生图。因此 Jobs renderer 的**编译和回归**已验证，但其独立视觉场景、差分阈值和 `enforce` 策略仍是未完成工作；T6.6 继续保持未勾选。
