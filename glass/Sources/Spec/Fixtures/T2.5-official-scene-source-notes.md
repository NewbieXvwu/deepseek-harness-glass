# T2.5 官方交互场景来源笔记

**锁定基线：** `deepseek-ai/deepseek-harness@99f6f02fecdb7dff40c3fbc9470f5907c29f74ca`。

本笔记记录用于 `official-interaction-scenes.json` 的直接官方源码证据。所有相对路径均相对于锁定官方仓库根目录；实际可执行目录 gate 会验证其中的 e2e、replay fixture 和稳定 ARIA baseline 文件存在。

| 场景域 | 官方来源 | 关键 fixture / baseline | 已提取的交互契约 |
|---|---|---|---|
| 启动、首次发送、流式、reload、dark cascade | `apps/web/tests/lifecycle-chrome.e2e.ts:47-57,156-275` | `snapshots/lifecycle-chrome/session.jsonl`；`hero.expected.md`；`reloaded.expected.md` | `launchWebScaffold` 隔离 Host、`connectFreshWorkspace`、hero 中发送、running follow-end、reload 后历史重放、`data-ds-dark-theme` alias token/painted surface flip 和恢复。 |
| 无 workspace welcome | `apps/web/tests/capture-official-welcome.e2e.ts:13-56` | CI 输出 `artifacts/official-webui/welcome-no-workspace-light.png/.json` | 固定 1280×840、en-US、light、DPR 1；等待 `Choose workspace` textbox；记录 root geometry、ARIA、console/page errors。 |
| Tool details / 详情列 | `apps/web/tests/navigation-panes.e2e.ts:80-133,226-289,400-420` | `snapshots/navigation-panes/seed.jsonl`；`trajectory.expected.md` | seeded rich session、Trajectory、tool row、`Event details` complementary、`Close details`；默认 details closed 的 grid contract。 |
| Approval takeover | `apps/web/tests/approval-composer.e2e.ts:56-65,72-180` | `snapshots/approval-composer/session.jsonl`；`ui.expected.md` | fresh workspace + Read Only；等待 `[data-approval-key]`；waiting-state ARIA；900×1000/700 cap、滚动区、两个 action；`Allow once` 后恢复 textarea。 |
| Question takeover | `apps/web/tests/question-composer.e2e.ts:45-55,62-182` | `snapshots/question-composer/session.jsonl`；`ui.expected.md`、`sidebar.expected.md`、`composed.expected.md`、`answered.expected.md` | 等待 `[data-question-key]`、selected sidebar warning、squeeze 900×520/440/380、选 Blue 和 custom answer、Enter 提交、恢复 regular composer。 |
| Queue actions | `apps/web/tests/queue-actions.e2e.ts:64-180` | `snapshots/live-interactions/session.jsonl`；`snapshots/queue-actions/{collapsed,editing,ui,preserved}.expected.md` | replay override hang + repeated response；queue expand、edit/remove、640px 宽的 dock/composer inset、stop 后 FIFO preservation。 |
| Settings | `apps/web/tests/settings-chrome.e2e.ts:30-130` | `snapshots/settings-chrome/dialog.expected.md`、`plugins.expected.md` | `zh-CN` 1680×1000；Settings dialog、General active、Models/Plugins navigation、Escape 和 header close；零 model call。 |
| 窄 sidebar rail / 三栏 | `packages/client/ui-layout/src/client/columns.ts:20-76` | `official-column-layout-fixtures.json` | `SIDEBAR_AUTO_COLLAPSE=1024`，1023px 进入 56px rail；side bar never concedes、details shrink then close。 |
| Details lifecycle | `apps/web/tests/details-session-lifecycle.e2e.ts:24-150` | `snapshots/lifecycle-chrome/session.jsonl` + `details-session-lifecycle-seed`；`handles.expected.md` | session select/reload/switch 都保持 details closed；inspector 显式打开/关闭；sidebar preference persistence。 |

> 截图状态说明：T2.5 目录为后续逐场景 capture 和 native pairing 定义了同状态 PNG 文件名和完整 capture contract。当前仅既有 `welcome-no-workspace-light` 有实际 CI 配对报告；其余场景在 renderer 实现和对应 official/native capture 完成前不得声明视觉验收通过。
