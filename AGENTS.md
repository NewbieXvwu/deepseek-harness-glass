# DeepSeek Harness Glass 研发智能体与工程守则 (AGENTS.md)

> **适用对象**：所有参与本项目开发、重构、审查或编写测试的 AI 智能体（Agent）及人类开发者。
> **本文件是硬约束，不是参考读物。** 只保留可执行的规则。

---

## 0. 红线（不可协商）

1. **生产代码禁止崩溃式构造**：`try!`、`fatalError`、`Dictionary(uniqueKeysWithValues:)`、无根据的强制解包。必须容错、降级、记录，绝不让非致命错误闪退客户端。
2. **禁止臆造官方契约**：协议字段、错误码、本地化文案、主题 token 一律以锁定的 DeepSeek Harness 源码为准；界面取值必须有多命名空间回退。
3. **热路径禁止二次编解码**；**大文本禁止回溯正则**，用 O(1) 前缀/后缀/切片。
4. **增量状态机是纯函数** `(State, Frame) -> State`：不抛错、不异步、不副作用；坏帧丢弃或对齐，禁止因局部错误清空全局状态。
5. **长连接消费 Task 双态**：握手期首帧失败立即上抛；消费期网络闪断/坏帧必须指数退避自愈重连，禁止静默 `return` 变植物人。
6. **测试只测行为**：禁止读项目自身 `.swift` 源码做正则/子串/行数/出现次数断言。门禁自测允许“篡改 fixture → 调用真实门禁 → 断言失败退出码或结构化产物”，但禁止断言门禁的错误文案。
7. **测试必须可证伪、可重构**：行为被破坏要红；纯重构/排版要绿；非 Happy Path（闪断、乱序、坏帧、竞态）优先覆盖。核心状态机必须有确定性混沌测试并断言不变量与时间预算。
8. **零警告零错误**：`swift build` 在 Swift 6 严格并发下必须干净。
9. **无消费者即垃圾**：新增类型、文件、协议、抽象、门禁、资源必须有现在的消费者；“先留着以后用”一律不接受。
10. **删除前必查**：任何子系统或文件删除前先看 `TODO.md` 的 `[ ]` 状态与全仓引用；开发中功能即使未接线也不得误删。

## 1. 默认立场：删 > 改 > 增

- 能删就删，能改就不加，能复用就不自造。
- 新增任何实体前，必须能回答四问；答不上来就不加：
  1. **谁消费它？** 现在，不是“将来”。
  2. **删掉它会有什么真实回归？** 有没有更小的替代？
  3. **仓库里已有等价实现吗？** 为什么不复用？
  4. **它是否只是把复杂度挪到别的层？**
- 禁止常驻的兼容层、过渡层、以后可能用的死代码；过渡物必须写明删除条件与到期任务。
- 同一逻辑出现第二遍应考虑复用；只被用一次且没有独立语义的抽象是负资产。

## 2. 改动前决策门（5 问）

1. 这是删、改还是增？为什么不能直接删？
2. 依据是运行态行为还是文本/猜测？协议与文案必须回到上游源码。
3. 失败路径怎么表现？会不会把非致命错误升级成崩溃或全局清空？
4. 测试怎么在行为被破坏时变红？重构时会不会假红？
5. 若涉及删除：`TODO.md` 状态、全仓引用、CI 与文档引用是否已检查？

## 3. 架构与并发红线

1. **模块分层**：`Core` 不引 AppKit/SwiftUI/WebView；`UI` 只依赖 `Core`，不越权调私有管道；`PluginPlane` 负责隔离沙箱，红区凭证与内部数据不得向 loopback 逃逸。
2. **Actor 边界**：非 Sendable 的流式迭代器禁止跨 Task/Actor 逃逸；流的消费与首帧协商在同一 Actor 上下文完成。
3. **无隐式 Sendable 捕获**：异步测试辅助禁止在普通闭包里可变捕获跨线程共享状态，用 Actor 隔离或显式等待。
4. **共享目录所有权**：组件只能创建并清理自己的隔离子目录，禁止在 `deinit` 里删除传入的共享根目录。
5. **系统 API 先读规范**：不确定平台桥接语义就查文档或官方源码，禁止凭直觉调用。

## 4. 完成定义（DoD）

提交前逐项确认，缺一不可：

- [ ] `swift build --package-path glass`：0 警告 0 错误。
- [ ] `swift test --package-path glass`：0 失败 0 崩溃；新增行为有可证伪测试。
- [ ] `python3 tools/check-markdown-links.py`：通过。
- [ ] **死代码自查**：本次新增的每个符号都有引用；本次删除的符号全仓无残留。
- [ ] **测试自查**：无镜像实现、无只断言常量的套娃、无依赖外部环境的硬崩溃（缺环境用 `XCTSkip`）。
- [ ] **文档自查**：移动或删除文件后链接与 `TODO.md` 引用同步。
- [ ] **规模自查**：优先净删除；只增不减的改动必须写清理由。

## 5. 自审命令

```bash
# 构建零警告 + 全量测试 + 文档链接
swift build --package-path glass
swift test --package-path glass
python3 tools/check-markdown-links.py

# 死代码自查：本次改动涉及的符号是否还有引用
grep -rn "<Symbol>" --include=*.swift glass

# 删除前自查：TODO 状态 + 全仓引用
grep -n "<PathOrSymbol>" TODO.md
grep -rn "<PathOrSymbol>" --include=*.swift --include=*.py --include=*.yml glass tools .github
```
